{
  InputStream is=null;
  FileOutputStream os=null;
  try {
    is=url.openStream();
    output.getParentFile().mkdirs();
    os=new FileOutputStream(output);
    copy(is,os);
  }
  finally {
    closeQuietly(is);
    closeQuietly(os);
  }
}
