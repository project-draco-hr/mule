{
synchronized (extensionModel) {
    if (extensionRegistry.getConfigurationProviders(extensionModel).isEmpty()) {
      registerConfigurationProvider(implicitConfigurationProviderFactory.createImplicitConfigurationProvider(extensionModel,muleEvent,muleContext));
    }
  }
}
