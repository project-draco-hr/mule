{
  try {
    if (event == null || VoidMuleEvent.getInstance().equals(event)) {
      event=getCurrentEvent();
      if (event == null || VoidMuleEvent.getInstance().equals(event)) {
        return;
      }
    }
    getNotificationHelper(muleContext.getNotificationManager()).fireNotification(source,event,event.getMessageSourceURI() != null ? event.getMessageSourceURI().toString() : null,flow,action);
  }
 catch (  Exception e) {
    logger.warn("Could not fire notification. Action: " + action,e);
  }
}
