{
  MuleServer muleServer=new MuleServer(new String[]{"-config","mule-config.xml","-appconfig","org/mule/test/spring/config1/test-app-config.properties"});
  muleServer.initialize();
  assertTrue(MuleServer.muleContext.getConfiguration().getWorkingDirectory().endsWith("tests/functional/target/.appT"));
}
