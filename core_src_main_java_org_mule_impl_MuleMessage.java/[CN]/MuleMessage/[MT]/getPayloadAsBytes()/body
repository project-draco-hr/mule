{
  assertAccess(READ);
  if (cache != null) {
    return cache;
  }
  byte[] result=(byte[])getPayload(byte[].class);
  if (DebugOptions.isCacheMessageAsBytes()) {
    cache=result;
  }
  return result;
}
