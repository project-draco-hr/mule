{
  Path directoryPath=directory == null ? Paths.get(config.getBaseDir()) : Paths.get(config.getBaseDir()).resolve(directory).toAbsolutePath();
  return resolveExistingPath(config,directoryPath.toAbsolutePath().toString());
}
