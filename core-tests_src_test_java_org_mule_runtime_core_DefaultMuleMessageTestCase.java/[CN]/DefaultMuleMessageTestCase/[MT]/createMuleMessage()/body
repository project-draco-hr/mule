{
  MutableMuleMessage previousMessage=new DefaultMuleMessage(AbstractMuleContextTestCase.TEST_PAYLOAD,DataType.builder(DataType.STRING).charset(getDefaultEncoding(muleContext)).build(),testAttributes,muleContext);
  previousMessage.setOutboundProperty("MuleMessage","MuleMessage");
  return previousMessage;
}
