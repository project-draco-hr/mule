{
  QueueSession queueSession=getTestQueueSession();
  logger.warn("SEDA Queue: " + queueSession.getQueue(service.getName() + ".service").size() + ", Outbound JMS consumer: "+ (NUM_MESSAGES - listener.countdownLatch.getCount()));
  assertEquals(numMessages,(NUM_MESSAGES - listener.countdownLatch.getCount()) + queueSession.getQueue(service.getName() + ".service").size());
}
