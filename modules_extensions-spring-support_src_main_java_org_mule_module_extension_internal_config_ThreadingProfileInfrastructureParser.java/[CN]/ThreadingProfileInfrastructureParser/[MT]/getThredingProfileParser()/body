{
  OrphanDefinitionParser parser=new OrphanDefinitionParser(ChainedThreadingProfile.class,true);
  parser.addMapping("poolExhaustedAction",ThreadingProfile.POOL_EXHAUSTED_ACTIONS).registerPostProcessor(new ConstructorReference(OBJECT_DEFAULT_SERVICE_THREADING_PROFILE));
  return parser;
}
