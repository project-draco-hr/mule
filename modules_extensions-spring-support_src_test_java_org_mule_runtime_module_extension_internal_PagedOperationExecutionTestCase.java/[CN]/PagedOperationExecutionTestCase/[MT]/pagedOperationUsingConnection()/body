{
  ConsumerIterator iterator=(ConsumerIterator)flowRunner("pagedOperationUsingConnection").run().getMessage().getPayload().getValue();
  while (iterator.hasNext()) {
    assertThat(iterator.next().toString(),containsString(SAUL_NEW_NUMBER));
  }
}
