{
  checkNotNull(artifact,"artifact cannot be null");
  final ArtifactDescriptorRequest request=new ArtifactDescriptorRequest(artifact,Collections.<RemoteRepository>emptyList(),null);
  return system.readArtifactDescriptor(session,request);
}
