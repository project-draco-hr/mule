{
  try {
    queueFile.close();
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
  orderedKeys.clear();
  fileTotalSpace=0;
  FileUtils.deleteQuietly(file);
  createQueueFile();
}
