{
  try {
    queueFile.close();
    orderedKeys.clear();
    fileTotalSpace=0;
    FileUtils.deleteQuietly(file);
    createQueueFile();
  }
 catch (  IOException e) {
    throw new MuleRuntimeException(e);
  }
}
