{
  this.file=new File(directory,filename);
  createQueueFile();
  try {
    createQueueFile();
  }
 catch (  Exception e) {
    this.file=new File(directory,toHex(filename));
    createQueueFile();
  }
  initialise();
}
