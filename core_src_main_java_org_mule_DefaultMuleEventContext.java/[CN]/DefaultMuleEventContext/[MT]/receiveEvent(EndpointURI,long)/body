{
  ImmutableEndpoint endpoint=getMuleContext().getRegistry().lookupEndpointFactory().getEndpoint(endpointUri,ImmutableEndpoint.ENDPOINT_TYPE_SENDER);
  return session.receiveEvent(endpoint,timeout);
}
