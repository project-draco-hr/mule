{
  OutboundEndpoint endpoint=getMuleContext().getRegistry().lookupEndpointFactory().getOutboundEndpoint(endpointUri);
  setRemoteSync(message,endpoint);
  return session.sendEvent(message,endpoint);
}
