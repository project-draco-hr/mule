{
  String oldExpectedType=expectedType;
  expectedType=newExpectedType;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,MulePackage.LEFT_FILTER_TYPE__EXPECTED_TYPE,oldExpectedType,expectedType));
}
