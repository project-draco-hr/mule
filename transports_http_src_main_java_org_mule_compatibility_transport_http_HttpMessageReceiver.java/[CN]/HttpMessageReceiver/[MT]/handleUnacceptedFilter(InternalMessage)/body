{
  if (logger.isDebugEnabled()) {
    logger.debug("Message request '" + message.getInboundProperty(HttpConnector.HTTP_REQUEST_PROPERTY) + "' is being rejected since it does not match the filter on this endpoint: "+ endpoint);
  }
  return InternalMessage.builder(message).addOutboundProperty(HTTP_STATUS_PROPERTY,String.valueOf(SC_NOT_ACCEPTABLE)).build();
}
