{
  if (!receiver.stopped.get()) {
    running=true;
    try {
      receiver.connect();
      receiver.poll();
    }
 catch (    InterruptedException e) {
      receiver.stop();
    }
catch (    Exception e) {
      receiver.handleException(e);
    }
 finally {
      running=false;
    }
  }
}
