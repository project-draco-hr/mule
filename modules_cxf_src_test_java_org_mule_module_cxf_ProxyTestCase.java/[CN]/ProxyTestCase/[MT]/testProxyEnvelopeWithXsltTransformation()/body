{
  MuleClient client=muleContext.getClient();
  MuleMessage result=client.send("http://localhost:" + dynamicPort.getNumber() + "/services/envelope-xslt-proxy",getTestMuleMessage(msg),HTTP_REQUEST_OPTIONS);
  assertTrue(result.getPayloadAsString().contains(msg));
}
