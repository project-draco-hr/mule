{
  MuleClient client=muleContext.getClient();
  MuleMessage result=client.send("http://localhost:" + dynamicPort.getNumber() + "/services/envelope-xslt-proxy",new DefaultMuleMessage(msg,muleContext));
  assertTrue(result.getPayloadAsString().contains(msg));
}
