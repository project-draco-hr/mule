{
  Export exportAnnotation=extractAnnotation(describingContext.getExtensionDeclarer().getDeclaration(),Export.class);
  if (exportAnnotation != null) {
    describingContext.getExtensionDeclarer().withModelProperty(new ExportModelProperty(copyOf(exportAnnotation.classes()),copyOf(exportAnnotation.resources())));
  }
}
