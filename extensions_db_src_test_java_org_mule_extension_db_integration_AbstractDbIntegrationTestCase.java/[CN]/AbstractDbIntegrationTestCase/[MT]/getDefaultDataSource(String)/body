{
  try {
    ConfigurationProvider configurationProvider=muleContext.getRegistry().get(configName);
    DbConnectionProvider connectionProvider=(DbConnectionProvider)configurationProvider.get(testEvent()).getConnectionProvider().get();
    return connectionProvider.getConfiguredDataSource();
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
}
