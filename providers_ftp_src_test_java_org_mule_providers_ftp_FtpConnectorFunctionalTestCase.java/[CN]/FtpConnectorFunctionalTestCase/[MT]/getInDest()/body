{
  try {
    return new MuleEndpointURI("ftp://" + USERNAME + ":"+ PASSWORD+ "@"+ HOSTNAME+ "/"+ ADDRESS);
  }
 catch (  MalformedEndpointException e) {
    fail(e.getMessage());
    return null;
  }
}
