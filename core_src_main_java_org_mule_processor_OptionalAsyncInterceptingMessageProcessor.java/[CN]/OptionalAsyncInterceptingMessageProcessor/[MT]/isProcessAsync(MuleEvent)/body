{
  boolean forceSync=Boolean.TRUE.equals(event.getMessage().getInvocationProperty(MuleProperties.MULE_FORCE_SYNC_PROPERTY));
  return !forceSync && doThreading && !event.getEndpoint().getExchangePattern().hasResponse()&& !event.getEndpoint().getTransactionConfig().isTransacted();
}
