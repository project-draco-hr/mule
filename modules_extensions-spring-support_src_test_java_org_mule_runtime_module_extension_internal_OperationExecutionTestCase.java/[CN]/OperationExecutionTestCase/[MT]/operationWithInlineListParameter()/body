{
  List<String> response=flowRunner("knockManyWithInlineList").withPayload("").withFlowVariable("victim","Saul").run().getMessage().getPayload();
  assertThat(response,Matchers.contains(knock("Inline Skyler"),knock("Saul")));
}
