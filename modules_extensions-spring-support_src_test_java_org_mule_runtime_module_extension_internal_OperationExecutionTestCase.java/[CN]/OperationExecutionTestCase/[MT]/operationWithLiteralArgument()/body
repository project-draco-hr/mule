{
  Event event=flowRunner("literalEcho").withPayload(EMPTY).run();
  assertThat(((ParameterResolver<String>)event.getMessage().getPayload().getValue()).getExpression(),is(Optional.of("#[money]")));
}
