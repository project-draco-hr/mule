{
  StringToEmailMessage trans=createObject(StringToEmailMessage.class);
  ImmutableEndpoint endpoint=muleContext.getRegistry().lookupEndpointFactory().getOutboundEndpoint("smtp://a:a@a.com");
  try {
    endpoint.getConnector().initialise();
  }
 catch (  IllegalStateException e) {
    assertNotNull(e);
  }
  trans.setEndpoint(endpoint);
  return trans;
}
