{
  String epUri;
  if (ep instanceof DynamicOutboundEndpoint) {
    Flow flow=getTestFlow();
    epUri=muleContext.getExpressionManager().parse(ep.getAddress(),new DefaultMuleEvent(create(flow),message,flow),true);
  }
 else {
    epUri=ep.getAddress();
  }
  assertEquals(resultUri,epUri);
}
