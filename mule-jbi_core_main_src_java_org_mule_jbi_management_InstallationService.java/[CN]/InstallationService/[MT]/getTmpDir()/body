{
  while (true) {
    String s=Integer.toHexString(++counter);
    while (s.length() < 8) {
      s="0" + s;
    }
    File f=new File(this.tmpDir,s);
    if (!f.exists()) {
      return f;
    }
  }
}
