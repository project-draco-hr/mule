{
  String path=RegistryContext.getConfiguration().getWorkingDirectory() + File.separator + MuleConfiguration.DEFAULT_QUEUE_STORE;
  store=FileUtils.newFile(path).getCanonicalFile();
  store.mkdirs();
}
