{
  String oldClassName=className;
  className=newClassName;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,SchemaPackage.CONTAINER_CONTEXT_TYPE__CLASS_NAME,oldClassName,className));
}
