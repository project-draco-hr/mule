{
  assertFalse(service.isStarted());
  assertFalse(service.isPaused());
  service.dispose();
  service.initialise();
  assertFalse(service.isStarted());
  service.dispose();
  assertFalse(service.isStarted());
  service.dispose();
}
