{
  ConnectionStrategy defaultConnectionStrategy=(ConnectionStrategy)getRegistry().lookupObject(MuleProperties.OBJECT_DEFAULT_CONNECTION_STRATEGY);
  try {
    return (ConnectionStrategy)BeanUtils.cloneBean(defaultConnectionStrategy);
  }
 catch (  Exception e) {
    throw new MuleRuntimeException(CoreMessages.failedToClone("Connection Strategy"),e);
  }
}
