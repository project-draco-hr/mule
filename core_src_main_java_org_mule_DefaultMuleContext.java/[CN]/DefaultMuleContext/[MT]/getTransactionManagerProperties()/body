{
  Collection temp=registryBroker.lookupObjects(TransactionManagerProperties.class);
  if (temp.size() > 0)   return (TransactionManagerProperties)temp.iterator().next();
 else   return new TransactionManagerProperties();
}
