{
  if (parentNode.getPrefix() == null) {
    return parentNode.getNamespaceURI().equals("http://www.mulesoft.org/schema/mule/core") ? CORE_NAMESPACE : POLICY_ROOT_ELEMENT;
  }
 else {
    return parentNode.getPrefix();
  }
}
