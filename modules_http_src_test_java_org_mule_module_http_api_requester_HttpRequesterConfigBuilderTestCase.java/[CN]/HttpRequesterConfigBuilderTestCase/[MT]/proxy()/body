{
  when(mockProxyConfig.getHost()).thenReturn("anyHost");
  when(mockProxyConfig.getPort()).thenReturn(9090);
  assertThat(builder.setProxyConfig(mockProxyConfig).build().getProxyConfig(),is(mockProxyConfig));
}
