{
  String file="http://some.domain.com/artifact.zip" + SEPARATOR + "classes"+ SEPARATOR+ "some-non-existing-path.txt";
  URL urlWithMuleArtifactProtocol=new URL(PROTOCOL,"",-1,file);
  urlWithMuleArtifactProtocol.openStream();
}
