{
  InputStream is;
  try {
    is=ClassUtils.getResourceAsStream(configResource,getClass(),true,true);
    return is;
  }
 catch (  IOException e) {
    throw new ConfigurationException(new Message(Messages.CANT_LOAD_X_FROM_CLASSPATH_FILE,configResource),e);
  }
}
