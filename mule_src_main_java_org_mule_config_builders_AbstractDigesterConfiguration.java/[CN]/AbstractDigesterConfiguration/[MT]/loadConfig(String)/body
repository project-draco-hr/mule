{
  InputStream is=ClassUtils.getResourceAsStream(configResource,getClass(),true,true);
  if (is != null) {
    return is;
  }
 else {
    throw new ConfigurationException(new Message(Messages.CANT_LOAD_X_FROM_CLASSPATH_FILE,configResource));
  }
}
