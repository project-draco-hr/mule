{
  if (event != null || event instanceof VoidMuleEvent) {
    return new ProcessorExecutorFactory().createProcessorExecutor(event,singletonList(next),createExceptionTransformerExecutionTemplate(),false).execute();
  }
 else {
    return event;
  }
}
