{
  if (event != null || event instanceof VoidMuleEvent) {
    return new ProcessorExecutorFactory().createProcessorExecutor(event,singletonList(next),createExceptionTransformerExecutionTemplate(),false,flowConstruct).execute();
  }
 else {
    return event;
  }
}
