{
  final MuleMessage message=MuleMessage.builder().payload(TEST_PAYLOAD).build();
  final MuleEvent event=MuleEvent.builder(executionContextWithCorrelation).message(message).flow(flow).correlation(new Correlation(null,6)).build();
  assertThat(event.getCorrelationId(),is(CORRELATION_ID + ":" + "6"));
}
