{
  if (isStartable) {
    try {
      ((Startable)componentObject.get()).start();
      started=true;
    }
 catch (    Exception e) {
      throw new DefaultMuleException(CoreMessages.failedToStart("Service: " + component.getService().getName()),e);
    }
  }
 else {
    started=true;
  }
}
