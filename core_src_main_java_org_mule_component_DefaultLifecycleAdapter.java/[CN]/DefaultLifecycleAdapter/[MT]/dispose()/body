{
  if (isDisposable) {
    try {
      ((Disposable)componentObject).dispose();
    }
 catch (    Exception e) {
      logger.error("failed to dispose: " + component.getService().getName(),e);
    }
  }
  disposed=true;
}
