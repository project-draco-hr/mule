{
  final DefaultMuleEvent event=new DefaultMuleEvent(new DefaultMuleMessage(RESPONSE_MESSAGE,muleContext),getTestFlow(),getTestSession(null,muleContext));
  event.populateFieldsFromInboundEndpoint(ep);
  return event;
}
