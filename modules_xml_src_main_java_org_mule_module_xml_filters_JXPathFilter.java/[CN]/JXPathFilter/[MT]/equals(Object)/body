{
  if (this == obj)   return true;
  if (obj == null || getClass() != obj.getClass())   return false;
  final JXPathFilter other=(JXPathFilter)obj;
  return equal(expectedValue,other.expectedValue) && equal(contextProperties,other.contextProperties) && equal(namespaces,other.namespaces)&& equal(pattern,other.pattern)&& lenient == other.lenient;
}
