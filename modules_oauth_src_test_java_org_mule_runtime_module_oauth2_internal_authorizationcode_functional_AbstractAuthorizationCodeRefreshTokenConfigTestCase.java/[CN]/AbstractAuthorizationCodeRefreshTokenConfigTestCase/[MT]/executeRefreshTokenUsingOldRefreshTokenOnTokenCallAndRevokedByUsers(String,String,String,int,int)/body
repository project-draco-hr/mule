{
  configureResourceResponsesForRefreshToken(oauthConfigName,userId,resourceFailureStatusCode);
  wireMockRule.stubFor(post(urlEqualTo(RESOURCE_PATH)).withHeader(HttpHeaders.Names.AUTHORIZATION,containing(REFRESHED_ACCESS_TOKEN)).willReturn(aResponse().withStatus(tokenFailureStatusCode).withBody("")));
  runFlow(flowName,userId);
}
