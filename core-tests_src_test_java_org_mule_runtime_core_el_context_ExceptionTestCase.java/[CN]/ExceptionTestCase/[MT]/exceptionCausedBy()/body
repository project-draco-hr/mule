{
  MuleEvent event=getTestEvent("");
  MuleMessage message=event.getMessage();
  Flow flow=getTestFlow();
  MessagingException me=new MessagingException(CoreMessages.createStaticMessage(""),new DefaultMuleEvent(create(flow),message,ONE_WAY,flow),new IllegalAccessException());
  event.setError(ErrorBuilder.builder(me).build());
  assertTrue((Boolean)evaluate("exception.causedBy(java.lang.IllegalAccessException)",event));
}
