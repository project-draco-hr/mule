{
  FlowConstruct flow=mock(FlowConstruct.class);
  when(flow.getMuleContext()).thenReturn(muleContext);
  event=MuleEvent.builder(DefaultMessageContext.create(flow,TEST_CONNECTOR)).message(org.mule.runtime.core.api.MuleMessage.builder().payload("").attributes(attributes).build()).build();
  delegate=createReturnDelegate();
  when(operationContext.getEvent()).thenReturn(event);
}
