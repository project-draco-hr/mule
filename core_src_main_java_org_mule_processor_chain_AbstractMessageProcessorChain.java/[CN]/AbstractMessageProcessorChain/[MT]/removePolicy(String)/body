{
  final AroundPolicy policy=(AroundPolicy)CollectionUtils.find(this.policies,new BeanPropertyValueEqualsPredicate("name",policyName));
  if (policy == null) {
    return null;
  }
  this.policies.remove(policy);
  return policy;
}
