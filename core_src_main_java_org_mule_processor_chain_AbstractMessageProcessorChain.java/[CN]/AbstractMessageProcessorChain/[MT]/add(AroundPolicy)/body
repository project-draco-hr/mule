{
  if (findPolicy(policy.getName()) != null) {
    final String msg=String.format("There's already a policy registered under name [%s] for chain [%s]:%s",policy.getName(),getName(),this);
    throw new IllegalArgumentException(msg);
  }
  this.policies.add(policy);
}
