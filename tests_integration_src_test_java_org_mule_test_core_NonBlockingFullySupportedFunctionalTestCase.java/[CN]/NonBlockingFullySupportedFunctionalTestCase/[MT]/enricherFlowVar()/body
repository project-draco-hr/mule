{
  Event result=flowRunner("enricherFlowVar").withPayload(TEST_MESSAGE).withExchangePattern(getMessageExchnagePattern()).nonBlocking().run();
  assertThat(result.getVariable(FOO).getValue(),is(equalTo(TEST_MESSAGE)));
}
