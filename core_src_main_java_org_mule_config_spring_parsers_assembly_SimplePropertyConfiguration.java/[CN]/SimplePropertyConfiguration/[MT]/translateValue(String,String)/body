{
  ValueMap vm=(ValueMap)valueMappings.get(name);
  if (vm != null) {
    Object v=vm.getValue(value);
    if (v != null) {
      return v.toString();
    }
 else {
      return value;
    }
  }
 else {
    return value;
  }
}
