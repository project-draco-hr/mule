{
  OutputStream os=msg.getContent(OutputStream.class);
  if (os != null) {
    os.close();
  }
  if (!isProxy(msg)) {
    InputStream in=msg.getContent(InputStream.class);
    if (in != null) {
      in.close();
    }
  }
}
