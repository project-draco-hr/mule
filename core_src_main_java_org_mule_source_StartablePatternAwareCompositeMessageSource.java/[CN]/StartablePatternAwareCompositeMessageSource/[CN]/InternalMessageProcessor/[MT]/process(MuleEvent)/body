{
  if (started.get()) {
    return listener.process(event);
  }
 else {
    log.warn("Message " + event + " was recieved from MessageSource, but MessageSourceAggregator "+ this+ " is stopped.  Message will be discarded.");
    return null;
  }
}
