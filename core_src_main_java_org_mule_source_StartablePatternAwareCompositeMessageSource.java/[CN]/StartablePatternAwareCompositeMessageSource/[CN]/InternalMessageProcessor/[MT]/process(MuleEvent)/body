{
  if (started.get()) {
    return listener.process(event);
  }
 else {
    log.warn("Message " + event + " was recieved from MessageSource, but message source "+ this+ " is stopped.  Message will be discarded.");
    return null;
  }
}
