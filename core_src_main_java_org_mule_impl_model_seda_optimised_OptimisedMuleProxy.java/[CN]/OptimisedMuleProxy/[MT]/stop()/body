{
  checkDisposed();
  if (started && umo instanceof Stoppable) {
    started=false;
    try {
      ((Stoppable)umo).stop();
    }
 catch (    Exception e) {
      throw new ModelException(CoreMessages.failedToStop("Component '" + descriptor.getName() + "'"),e);
    }
  }
}
