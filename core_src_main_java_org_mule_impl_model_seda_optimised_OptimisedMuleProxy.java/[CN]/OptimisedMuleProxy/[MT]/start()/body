{
  checkDisposed();
  if (!started && umo instanceof Startable) {
    try {
      ((Startable)umo).start();
      started=true;
    }
 catch (    Exception e) {
      throw new ModelException(CoreMessages.failedToStart("Component '" + descriptor.getName() + "'"),e);
    }
  }
}
