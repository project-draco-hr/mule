{
  if (getFilter() == null) {
    return true;
  }
  try {
    message.applyTransformers(null,transformers);
  }
 catch (  MuleException e) {
    throw new RoutingException(CoreMessages.transformFailedBeforeFilter(),message,routes.get(0),e);
  }
  return getFilter().accept(message);
}
