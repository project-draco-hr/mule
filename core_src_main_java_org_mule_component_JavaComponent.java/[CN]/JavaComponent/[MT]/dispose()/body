{
  checkDisposed();
  if (umo.isStarted()) {
    try {
      umo.stop();
    }
 catch (    MuleException e) {
      logger.error(CoreMessages.failedToStop("Service '" + service.getName() + "'"),e);
    }
  }
  umo.dispose();
}
