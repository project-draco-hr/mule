{
  String state=this.getState();
  if (state != null) {
    state=String.format(OAuthProperties.EVENT_STATE_TEMPLATE,event.getId()) + this.toString(event,state);
    extraParameters.put("state",state);
  }
}
