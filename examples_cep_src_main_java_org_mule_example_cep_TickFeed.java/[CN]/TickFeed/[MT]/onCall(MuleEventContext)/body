{
  if (lines.hasNext()) {
    Object[] results=lineFormat.parse(lines.next());
    StockTick tick=new StockTick((String)results[1],((Number)results[2]).doubleValue(),((Number)results[0]).longValue());
    logger.info("New Stock Tick: " + tick);
    return tick;
  }
 else {
    return NullPayload.getInstance();
  }
}
