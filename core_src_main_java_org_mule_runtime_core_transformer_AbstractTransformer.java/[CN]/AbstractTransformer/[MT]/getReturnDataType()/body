{
  if (returnType == null) {
synchronized (this) {
      if (returnType == null) {
        returnType=DataType.builder().charset(getDefaultEncoding(muleContext)).build();
      }
    }
  }
  return returnType;
}
