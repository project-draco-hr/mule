{
  when(mockServiceRegistry.lookupProviders(ConnectivityTestingStrategy.class,any())).thenReturn(asList(mock(ConnectivityTestingStrategy.class)));
  addExtension().setArtifactConfiguration(getArtifactConfiguration());
  builder.build();
}
