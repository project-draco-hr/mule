{
  try {
    managementContext.getRegistry().registerService(MuleTestUtils.createDescriptor(ComponentWithoutInterfaces.class.getName(),"testComponentWithoutInterfaces",getComponentWithoutInterfacesEndpoint(),null,null,managementContext),managementContext);
    fail();
  }
 catch (  UMOException e) {
    e=ExceptionHelper.getRootMuleException(e);
    assertTrue(e instanceof InitialisationException);
  }
}
