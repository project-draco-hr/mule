{
  response.setStatus(HttpServletResponse.SC_METHOD_NOT_ALLOWED);
  try {
    response.getWriter().write(HttpMessages.methodNotAllowed(HttpConstants.METHOD_OPTIONS).toString() + HttpConstants.CRLF);
  }
 catch (  Exception e) {
    handleException(e,e.getMessage(),response);
  }
}
