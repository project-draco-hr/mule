{
  long now=System.currentTimeMillis();
  if (manager.isReady())   if (now - manager.getLastRequestTime() > minInterval && manager.getRequestCount() > 0)   return true;
 else   if (now - manager.getLastRequestTime() > maxInterval)   return true;
  return false;
}
