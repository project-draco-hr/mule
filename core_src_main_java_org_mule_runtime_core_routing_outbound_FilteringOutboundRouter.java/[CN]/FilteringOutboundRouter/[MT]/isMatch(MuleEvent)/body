{
  if (getFilter() == null) {
    return true;
  }
  event=MuleEvent.builder(event).message(muleContext.getTransformationService().applyTransformers(event.getMessage(),null,transformers)).build();
  return getFilter().accept(event);
}
