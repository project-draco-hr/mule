{
  if (context != null) {
    context.stop();
    try {
      if (!(context.isDisposed() || context.isDisposing())) {
        context.dispose();
      }
    }
  finally {
      context=null;
    }
  }
}
