{
  if (muleContext != null) {
    try {
      if (!(muleContext.isDisposed() || muleContext.isDisposing())) {
        muleContext.dispose();
      }
    }
  finally {
      muleContext=null;
      muleContextRef.unregister();
    }
  }
}
