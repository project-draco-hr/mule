{
  for (int i=0; i < handler.getTransferDataFlavors().length; i++) {
    register(handler.getTransferDataFlavors()[i].getMimeType(),handler.getTransferDataFlavors()[i].getDefaultRepresentationClass(),handler);
  }
}
