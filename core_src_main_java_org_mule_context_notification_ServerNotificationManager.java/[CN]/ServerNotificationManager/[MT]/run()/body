{
  while (!disposed.get()) {
    try {
      ServerNotification notification=(ServerNotification)eventQueue.poll(MuleServer.getMuleContext().getConfiguration().getDefaultQueueTimeout(),TimeUnit.MILLISECONDS);
      if (notification != null) {
        notifyListeners(notification);
      }
    }
 catch (    InterruptedException e) {
    }
  }
}
