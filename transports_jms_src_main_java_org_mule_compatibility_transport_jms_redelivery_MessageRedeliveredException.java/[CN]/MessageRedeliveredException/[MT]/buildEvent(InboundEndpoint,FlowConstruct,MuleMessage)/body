{
  final MuleEvent event=MuleEvent.builder(create(flow,"MessageRedeliveredException")).message(muleMessage).flow(flow).build();
  populateFieldsFromInboundEndpoint(event,endpoint);
  return event;
}
