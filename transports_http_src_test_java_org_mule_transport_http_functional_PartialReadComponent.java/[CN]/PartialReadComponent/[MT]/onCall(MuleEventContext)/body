{
  InputStream stream=(InputStream)eventContext.getMuleContext().getTransformationService().getPayload(eventContext.getMessage(),DataTypeFactory.create(InputStream.class));
  stream.read();
  return "Hello";
}
