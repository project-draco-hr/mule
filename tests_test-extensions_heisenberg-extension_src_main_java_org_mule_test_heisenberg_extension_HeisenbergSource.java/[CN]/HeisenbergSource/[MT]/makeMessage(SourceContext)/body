{
  if (initialBatchNumber < 0) {
    sourceContext.getExceptionCallback().onException(new RuntimeException(INITIAL_BATCH_NUMBER_ERROR_MESSAGE));
  }
  return Message.builder().payload(format("Meth Batch %d. If found by DEA contact %s",++initialBatchNumber,connection.getSaulPhoneNumber())).build();
}
