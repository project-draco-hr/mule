{
  InternalMessage response=muleContext.getClient().send("http://localhost:" + httpPort.getNumber() + "/services/Echo",getTestMuleMessage(SAMPLE_REQUEST),HTTP_REQUEST_OPTIONS).getRight();
  assertTrue(getPayloadAsString(response).contains("bla"));
}
