{
  try {
    return org.mule.metadata.utils.MetadataTypeUtils.getTypeId(metadataType).orElse(metadataType.getMetadataFormat().equals(JAVA) ? getType(metadataType).getName() : "");
  }
 catch (  Exception e) {
    return "";
  }
}
