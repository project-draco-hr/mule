{
  runTestFailingWithExpectedFlowConstructInvalidException(new Callable<WSProxy>(){
    @Override public WSProxy call() throws Exception {
      return new WSProxy("testBlankWsdlContents",muleContext,getTestInboundEndpoint(MessageExchangePattern.REQUEST_RESPONSE),MuleTestUtils.getTestOutboundEndpoint(MessageExchangePattern.REQUEST_RESPONSE,muleContext),noMessageProcessors,noMessageProcessors,"");
    }
  }
);
}
