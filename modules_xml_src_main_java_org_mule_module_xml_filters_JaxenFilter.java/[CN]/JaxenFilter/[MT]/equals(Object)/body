{
  if (this == obj)   return true;
  if (obj == null || getClass() != obj.getClass())   return false;
  final JaxenFilter other=(JaxenFilter)obj;
  return equal(expectedValue,other.expectedValue) && equal(contextProperties,other.contextProperties) && equal(namespaces,other.namespaces)&& equal(pattern,other.pattern);
}
