{
  if (name == null)   return null;
  int i=name.indexOf(":");
  QName qname=null;
  if (i > -1) {
    qname=new QName("uri:" + defaultOperation.substring(0,i),defaultOperation.substring(i + 1));
  }
 else {
    qname=new QName(defaultOperation);
  }
  return qname;
}
