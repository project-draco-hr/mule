{
  if (logger.isDebugEnabled()) {
    logger.debug(eventContext);
  }
  String requestPath=parseHttpRequestProperty(eventContext.getMessage().getInboundProperty(HttpConnector.HTTP_REQUEST_PROPERTY,StringUtils.EMPTY));
  if (requestPath.indexOf('?') > -1) {
    return generateWSDLOrXSD(eventContext,requestPath);
  }
 else {
    return sendToDestination(eventContext);
  }
}
