{
  SimpleService simpleService=new SimpleService(muleContext,name,buildInboundEndpoint(),getOrBuildComponent());
  if (exceptionListener != null) {
    simpleService.setExceptionListener(exceptionListener);
  }
  return simpleService;
}
