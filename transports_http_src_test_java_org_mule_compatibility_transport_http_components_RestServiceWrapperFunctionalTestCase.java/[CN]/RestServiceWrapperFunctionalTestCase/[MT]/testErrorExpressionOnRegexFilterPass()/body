{
  InternalMessage result=muleContext.getClient().send("restServiceEndpoint2",InternalMessage.builder().payload(TEST_REQUEST).build()).getRight();
  assertEquals("echo=" + TEST_REQUEST,getPayloadAsString(result));
}
