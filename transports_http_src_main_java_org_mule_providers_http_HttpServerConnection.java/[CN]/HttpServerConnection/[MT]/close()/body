{
  try {
    if (socket != null) {
      if (logger.isDebugEnabled()) {
        logger.debug("Closing: " + socket);
      }
      socket.close();
    }
  }
 catch (  IOException e) {
  }
 finally {
    socket=null;
  }
}
