{
  checkDisposed();
  if (started && pojoService instanceof Stoppable) {
    started=false;
    try {
      ((Stoppable)pojoService).stop();
    }
 catch (    Exception e) {
      throw new ModelException(CoreMessages.failedToStop("Component '" + component.getName() + "'"),e);
    }
  }
}
