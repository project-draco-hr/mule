{
  if (responseProcessor == null || !isEventValid(event)) {
    return event;
  }
 else {
    return new CopyOnNullNonBlockingProcessorExecutor(event,Collections.singletonList(responseProcessor),MessageProcessorExecutionTemplate.createExecutionTemplate(),true).execute();
  }
}
