{
  if (responseProcessor == null || !isEventValid(response)) {
    return response;
  }
 else {
    return new CopyOnNullNonBlockingProcessorExecutor(response,singletonList(responseProcessor),messageProcessorExecutionTemplate,true).execute();
  }
}
