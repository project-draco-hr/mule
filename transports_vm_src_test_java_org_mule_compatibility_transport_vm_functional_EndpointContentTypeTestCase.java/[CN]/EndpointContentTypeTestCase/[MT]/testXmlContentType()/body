{
  InternalMessage result=client.send("vm://in1?connector=vm-in1",InternalMessage.builder().payload("<OK/>").mediaType(MediaType.XML).build()).getRight();
  assertNotNull(result.getExceptionPayload());
  assertTrue(result.getExceptionPayload().getException() instanceof MessagingException);
}
