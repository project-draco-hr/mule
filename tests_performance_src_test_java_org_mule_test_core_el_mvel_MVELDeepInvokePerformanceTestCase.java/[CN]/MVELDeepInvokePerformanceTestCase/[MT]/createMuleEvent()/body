{
  Flow flow;
  try {
    flow=getTestFlow();
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  return Event.builder(DefaultEventContext.create(flow,TEST_CONNECTOR)).message(InternalMessage.builder().payload(payload).build()).exchangePattern(ONE_WAY).flow(flow).build();
}
