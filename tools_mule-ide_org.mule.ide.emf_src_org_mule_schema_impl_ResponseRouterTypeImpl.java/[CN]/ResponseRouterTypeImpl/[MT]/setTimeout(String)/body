{
  String oldTimeout=timeout;
  timeout=newTimeout;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,SchemaPackage.RESPONSE_ROUTER_TYPE__TIMEOUT,oldTimeout,timeout));
}
