{
  try {
    ObjectStoreManager osManager=this.muleContext.getRegistry().lookupObject(ObjectStoreManager.class);
    this.refreshedTokens=osManager.getObjectStore("RefreshTokenStore",false,0,DEFAULT_EXPIRATION,DEFAULT_EXPIRATION);
  }
 catch (  RegistrationException e) {
    throw new InitialisationException(e,this);
  }
}
