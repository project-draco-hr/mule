{
  InputStream is=null;
  try {
    is=new BufferedInputStream(new FileInputStream(fileName));
    Properties p=new Properties();
    p.load(is);
    return p;
  }
 catch (  Exception ex) {
    throw ex;
  }
 finally {
    if (is != null) {
      is.close();
    }
  }
}
