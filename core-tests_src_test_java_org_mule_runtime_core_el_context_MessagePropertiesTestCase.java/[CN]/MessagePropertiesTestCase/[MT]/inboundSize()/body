{
  MuleMessage message=event.getMessage();
  mock(DataHandler.class);
  event=MuleEvent.builder(event).message(MuleMessage.builder(message).addInboundProperty("foo","abc").addInboundProperty("bar","xyz").build()).build();
  assertEquals(2,evaluate("message.inboundProperties.size()",event));
}
