{
  String recipients="vm://recipient1, vm://recipient1, vm://recipient3";
  logger.debug("Setting recipients to '" + recipients + "'");
  return MuleMessage.builder(event.getMessage()).addOutboundProperty(ExpressionRecipientList.DEFAULT_SELECTOR_PROPERTY,recipients).build();
}
