{
  String oldFactory=factory;
  factory=newFactory;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,SchemaPackage.TRANSACTION_MANAGER_TYPE__FACTORY,oldFactory,factory));
}
