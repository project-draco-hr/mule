{
  Event event=flowRunner(flowName).withPayload(TEST_MESSAGE).withFlowVariable("user",user).withFlowVariable("password",password).withFlowVariable("preemptive",preemptive).run();
  assertThat(getPayloadAsString(event.getMessage()),equalTo(DEFAULT_RESPONSE));
}
