{
  try {
    startMuleContext();
  }
 catch (  MuleException mExp) {
    Throwable e=mExp;
    while (e.getCause() != null) {
      e=e.getCause();
    }
    assertTrue(e instanceof IllegalArgumentException);
  }
}
