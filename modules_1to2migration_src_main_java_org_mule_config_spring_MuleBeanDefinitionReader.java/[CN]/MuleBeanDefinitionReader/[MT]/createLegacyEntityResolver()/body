{
  if (dtdResolver == null) {
    MuleDtdResolver muleSpringResolver=new MuleDtdResolver(MULE_SPRING_DTD,MULE_MIGRATION_STYLE_SHEET,new BeansDtdResolver());
    dtdResolver=new MuleDtdResolver(MULE_DTD,MULE_MIGRATION_STYLE_SHEET,muleSpringResolver);
  }
  return dtdResolver;
}
