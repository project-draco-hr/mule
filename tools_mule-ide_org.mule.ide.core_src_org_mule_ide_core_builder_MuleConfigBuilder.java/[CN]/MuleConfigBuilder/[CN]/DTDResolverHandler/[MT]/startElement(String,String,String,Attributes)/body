{
  if (seenRoot) {
    super.startElement(uri,localName,qName,attributes);
  }
 else {
    if (!qName.equals("mule-configuration"))     throw new WrongRootException("Only Mule Configurations are checked here, root-element '" + qName + "' not supported");
    seenRoot=true;
  }
}
