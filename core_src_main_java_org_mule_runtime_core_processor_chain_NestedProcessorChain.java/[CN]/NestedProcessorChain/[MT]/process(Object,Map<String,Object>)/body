{
  MuleEvent muleEvent=MuleEvent.builder(event).message(MuleMessage.builder().payload(payload).build()).flowVariables(properties).build();
  return chain.process(muleEvent).getMessage().getPayload();
}
