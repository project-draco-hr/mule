{
  Event muleEvent=Event.builder(event).message(InternalMessage.builder().payload(payload).build()).variables(properties).build();
  return chain.process(muleEvent).getMessage().getPayload().getValue();
}
