{
  if (isBlank(basePath)) {
    try {
      return client.printWorkingDirectory();
    }
 catch (    Exception e) {
      throw new MuleRuntimeException(createStaticMessage("FTP working dir was not specified and failed to resolve a default one"),e);
    }
  }
  return basePath;
}
