{
  ElementDescriptor policyElement=element.getChildByName("dynamic-config-policy");
  return policyElement == null ? DynamicConfigPolicy.getDefault(timeSupplier) : new DynamicConfigPolicy(getExpirationPolicy(policyElement));
}
