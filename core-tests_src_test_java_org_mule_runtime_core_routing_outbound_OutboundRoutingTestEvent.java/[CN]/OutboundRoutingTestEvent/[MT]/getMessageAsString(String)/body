{
  try {
    return (String)getMuleContext().getTransformationService().transform(message,DataTypeFactory.createWithEncoding(String.class,encoding)).getPayload();
  }
 catch (  Exception e) {
    throw new DefaultMuleException(e);
  }
}
