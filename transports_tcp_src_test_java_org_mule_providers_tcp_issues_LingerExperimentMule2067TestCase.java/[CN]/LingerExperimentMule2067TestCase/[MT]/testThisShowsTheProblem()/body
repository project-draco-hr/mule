{
  repeatOpenCloseClientServer(10,10,PORT,1000);
  repeatOpenCloseClientServer(10,10,PORT,100);
  repeatOpenCloseClientServer(10,10,PORT,10);
  repeatOpenCloseClientServer(10,10,PORT,1);
  repeatOpenCloseClientServer(10,10,PORT,0);
  repeatOpenCloseClientServer(10,10,PORT,NO_WAIT);
}
