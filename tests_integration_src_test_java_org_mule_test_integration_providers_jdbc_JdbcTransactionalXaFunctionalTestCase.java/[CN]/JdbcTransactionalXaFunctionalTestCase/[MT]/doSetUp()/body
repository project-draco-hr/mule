{
  txManager=Current.getCurrent();
  if (txManager == null) {
    new Jotm(true,false);
    txManager=Current.getCurrent();
  }
  super.doSetUp();
  managementContext.setTransactionManager(txManager);
}
