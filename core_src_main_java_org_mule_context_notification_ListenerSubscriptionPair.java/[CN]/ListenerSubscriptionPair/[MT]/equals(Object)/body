{
  if (this == obj) {
    return true;
  }
  if (obj == null || getClass() != obj.getClass()) {
    return false;
  }
  final ListenerSubscriptionPair other=(ListenerSubscriptionPair)obj;
  return equal(listener,other.listener) && equal(subscription,other.subscription) && equal(nullSubscription,other.nullSubscription);
}
