{
  event=MuleEvent.builder(event).message(MuleMessage.builder(event.getMessage()).addOutboundProperty(LIFECYCLE_TRACKER_PROCESSOR_PROPERTY,getTracker().toString()).build()).addFlowVariable(FLOW_CONSRUCT_PROPERTY,flowConstruct).build();
  return event;
}
