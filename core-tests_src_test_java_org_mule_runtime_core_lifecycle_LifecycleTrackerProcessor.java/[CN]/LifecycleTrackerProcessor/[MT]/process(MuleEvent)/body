{
  event.setMessage(MuleMessage.builder(event.getMessage()).addOutboundProperty(LIFECYCLE_TRACKER_PROCESSOR_PROPERTY,getTracker().toString()).build());
  event.setFlowVariable(FLOW_CONSRUCT_PROPERTY,flowConstruct);
  return event;
}
