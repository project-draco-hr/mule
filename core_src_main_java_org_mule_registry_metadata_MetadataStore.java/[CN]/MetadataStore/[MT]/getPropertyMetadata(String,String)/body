{
  if (!metadata.containsKey(className))   throw new MissingMetadataException(className);
  if (!((ObjectMetadata)metadata.get(className)).getProperties().containsKey(propertyName))   throw new MissingMetadataException(className,propertyName);
  return (PropertyMetadata)((Metadata)metadata.get(className)).getProperty(propertyName);
}
