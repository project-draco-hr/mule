{
  MuleEvent replyEvent=flowRunner("echo").withPayload("An unsigned message").withInboundProperties(createMessageProperties()).run();
  assertNotNull(replyEvent.getError());
  assertEquals(MESSAGE_EXCEPTION,replyEvent.getError().getDetailedDescription());
}
