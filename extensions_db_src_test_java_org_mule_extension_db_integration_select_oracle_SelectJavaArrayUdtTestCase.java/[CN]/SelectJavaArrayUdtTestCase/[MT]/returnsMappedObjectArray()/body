{
  Message response=flowRunner("returnsCustomArray").run().getMessage();
  assertRecords(response.getPayload().getValue(),new Record(new Field("CONTACT_NAME",CONTACT1.getName()),new Field("DETAILS",CONTACT1.getDetails())),new Record(new Field("CONTACT_NAME",CONTACT2.getName()),new Field("DETAILS",CONTACT2.getDetails())));
}
