{
  if (watchTimer != null) {
    watchTimer.shutdownNow();
  }
  Collections.reverse(applications);
  for (  Application application : applications) {
    try {
      application.stop();
      application.dispose();
    }
 catch (    Throwable t) {
      t.printStackTrace();
    }
  }
}
