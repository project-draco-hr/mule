{
  if (endpoint == null) {
    return;
  }
  String name=endpoint.getName();
  Long cpt=(Long)routed.get(name);
  long count=0;
  if (cpt != null) {
    count=cpt.longValue();
  }
  routed.put(name,Long.valueOf(++count));
  totalRouted++;
  totalReceived++;
}
