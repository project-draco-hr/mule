{
  MuleEvent event=getTestEvent(amount * 100);
  event.setFlowVariable("currency",currency);
  MuleEvent response=runFlow("greedy",event);
  assertThat(getPayloadAsString(response.getMessage()),equalTo(expectedResult));
}
