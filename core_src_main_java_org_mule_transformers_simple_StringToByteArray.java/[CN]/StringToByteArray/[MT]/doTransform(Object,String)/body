{
  if (src instanceof byte[]) {
    return src;
  }
 else {
    try {
      return ((String)src).getBytes(encoding);
    }
 catch (    UnsupportedEncodingException e) {
      throw new TransformerException(MessageFactory.createStaticMessage("Unable to convert String to byte[]."),e);
    }
  }
}
