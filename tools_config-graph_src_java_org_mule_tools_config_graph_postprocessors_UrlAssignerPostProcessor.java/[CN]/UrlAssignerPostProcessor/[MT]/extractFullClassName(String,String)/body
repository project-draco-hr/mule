{
  String className="";
  className=getAttribute(caption,"className");
  if (className == null) {
    className=getAttribute(caption,"implementation");
  }
  if (className == null) {
    className=header;
  }
  if (className == null) {
    className="";
  }
  return className;
}
