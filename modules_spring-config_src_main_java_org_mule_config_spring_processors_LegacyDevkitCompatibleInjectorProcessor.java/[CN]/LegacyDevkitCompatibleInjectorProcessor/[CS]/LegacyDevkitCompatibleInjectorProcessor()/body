{
  try {
    exclusionClass=ClassUtils.loadClass(PROCESS_ADAPTER_CLASS_NAME,getClass());
  }
 catch (  ClassNotFoundException e) {
    throw new MuleRuntimeException(createStaticMessage(String.format("Cannot start in Devkit legacy mode because %s class is not in classpath",PROCESS_ADAPTER_CLASS_NAME)),e);
  }
}
