{
  MuleClient client=new MuleClient();
  UMOMessage msg=client.send("http://localhost:33381/services/TestComponent",new MuleMessage(request));
  assertNotNull(msg);
  assertNotNull(msg.getPayload());
  assertEquals(response,msg.getPayloadAsString());
}
