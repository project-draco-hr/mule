{
  assertNotNull(response);
  assertEquals("JMS Priority should have not been honored.",4,response.getJMSPriority());
  assertEquals("JMS Delivery mode should have not been honored",DeliveryMode.NON_PERSISTENT,response.getJMSDeliveryMode());
}
