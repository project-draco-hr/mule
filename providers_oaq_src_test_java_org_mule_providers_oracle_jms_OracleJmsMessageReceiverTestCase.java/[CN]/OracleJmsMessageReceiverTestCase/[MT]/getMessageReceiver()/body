{
  getConnector();
  endpoint=new MuleEndpoint("jms://TEST_QUEUE",true);
  endpoint.setConnector(getConnector());
  Mock mockComponent=new Mock(UMOComponent.class);
  return new OracleJmsMessageReceiver((AbstractConnector)endpoint.getConnector(),(UMOComponent)mockComponent.proxy(),endpoint);
}
