{
  assertFalse(component.isStarted());
  assertFalse(component.isPaused());
  component.dispose();
  component.initialise();
  assertFalse(component.isStarted());
  component.dispose();
  assertFalse(component.isStarted());
  component.dispose();
}
