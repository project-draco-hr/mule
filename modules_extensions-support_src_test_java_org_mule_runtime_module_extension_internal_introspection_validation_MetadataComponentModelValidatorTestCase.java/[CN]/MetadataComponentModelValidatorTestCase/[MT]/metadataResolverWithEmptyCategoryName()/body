{
  exception.expect(IllegalModelDefinitionException.class);
  mockMetadataResolverFactory(sourceModel,new DefaultMetadataResolverFactory(EmptyCategoryName.class,emptyMap(),SimpleOutputResolver.class,SimpleOutputResolver.class));
  validator.validate(extensionModel);
}
