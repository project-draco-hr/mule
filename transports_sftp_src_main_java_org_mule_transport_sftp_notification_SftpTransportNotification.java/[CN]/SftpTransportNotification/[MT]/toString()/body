{
  return EVENT_NAME + "{action = " + getActionName(action)+ ", endpoint = "+ endpoint.getEndpointURI()+ ", info = "+ info+ ", size = "+ size+ ", resourceIdentifier = "+ resourceIdentifier+ ", timestamp = "+ timestamp+ ", serverId = "+ serverId+ ", message = "+ source+ "}";
}
