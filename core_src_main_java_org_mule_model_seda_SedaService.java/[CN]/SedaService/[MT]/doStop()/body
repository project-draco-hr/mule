{
  if (isPaused()) {
    resume();
  }
  if (queue != null && queue.size() > 0) {
    try {
      queueDraining.whenFalse(null);
    }
 catch (    InterruptedException e) {
    }
  }
  workManager.dispose();
}
