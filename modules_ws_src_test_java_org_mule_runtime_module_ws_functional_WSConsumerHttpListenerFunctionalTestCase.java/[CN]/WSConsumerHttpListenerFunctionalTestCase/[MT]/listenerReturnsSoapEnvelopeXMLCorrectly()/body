{
  MuleClient client=muleContext.getClient();
  MuleMessage response=client.send("http://localhost:" + clientPort.getValue(),getTestMuleMessage(ECHO_REQUEST),newOptions().method(POST.name()).build()).getRight();
  assertXMLEqual(EXPECTED_ECHO_RESPONSE,getPayloadAsString(response));
}
