{
  MuleClient client=muleContext.getClient();
  InternalMessage response=client.send("http://localhost:" + clientPort.getValue(),InternalMessage.of(ECHO_REQUEST),newOptions().method(POST.name()).build()).getRight();
  assertXMLEqual(EXPECTED_ECHO_RESPONSE,getPayloadAsString(response));
}
