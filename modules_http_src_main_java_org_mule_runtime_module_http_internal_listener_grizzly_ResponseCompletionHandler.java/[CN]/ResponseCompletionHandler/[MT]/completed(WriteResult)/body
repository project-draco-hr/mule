{
  try {
    if (!isDone) {
      sendResponse();
    }
 else {
      ctx.notifyDownstream(HttpServerFilter.RESPONSE_COMPLETE_EVENT);
      resume();
    }
  }
 catch (  IOException e) {
    failed(e);
  }
}
