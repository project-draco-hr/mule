{
  UMOImmutableEndpoint endpoint=manager.getRegistry().lookupOutboundEndpoint(url,MuleServer.getManagementContext());
  try {
    return endpoint.receive(timeout);
  }
 catch (  Exception e) {
    throw new ReceiveException(endpoint,timeout,e);
  }
}
