{
  UMOImmutableEndpoint endpoint=manager.getRegistry().lookupEndpointFactory().getOutboundEndpoint(url,MuleServer.getManagementContext());
  try {
    return endpoint.receive(timeout);
  }
 catch (  Exception e) {
    throw new ReceiveException(endpoint,timeout,e);
  }
}
