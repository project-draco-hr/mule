{
  MVELExpressionLanguage mvel=new MVELExpressionLanguage(muleContext);
  mvel.initialise();
  assertThat(mvel.evaluate("f('one','two')"),is("called param[0]=one,param[1]=two,app.name=" + muleContext.getConfiguration().getId()));
}
