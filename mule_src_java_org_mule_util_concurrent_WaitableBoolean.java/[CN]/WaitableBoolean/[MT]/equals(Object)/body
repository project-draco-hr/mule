{
  if (other == this) {
    return true;
  }
 else   if (other instanceof WaitableBoolean) {
synchronized (_lock) {
      return (_value == ((WaitableBoolean)other).get());
    }
  }
 else {
    return false;
  }
}
