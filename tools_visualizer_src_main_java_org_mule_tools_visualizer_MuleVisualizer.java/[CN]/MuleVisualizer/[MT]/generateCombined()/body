{
  env.setDoingCombinedGeneration(true);
  env.setEndpointRegistry(new EndpointRegistry(env));
  return generateGraph(1,env.getConfig().getFiles(),env.getConfig().getOutputDirectory(),env.getConfig().getCaption());
}
