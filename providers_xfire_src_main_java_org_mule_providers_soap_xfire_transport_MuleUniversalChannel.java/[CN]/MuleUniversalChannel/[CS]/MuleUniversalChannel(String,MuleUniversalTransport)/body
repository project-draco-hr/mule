{
  setTransport(transport);
  setUri(uri);
  try {
    this.client=new MuleClient();
  }
 catch (  UMOException ex) {
    throw new MuleRuntimeException(new Message(CoreMessageConstants.X_FAILED_TO_INITIALISE,"MuleClient"),ex);
  }
}
