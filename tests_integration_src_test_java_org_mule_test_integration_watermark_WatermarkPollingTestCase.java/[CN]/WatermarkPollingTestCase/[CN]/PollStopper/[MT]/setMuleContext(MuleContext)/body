{
  Map<String,MessageProcessorPollingConnector> connectors=context.getRegistry().lookupByType(MessageProcessorPollingConnector.class);
  for (  MessageProcessorPollingConnector connector : connectors.values()) {
    connector.setInitialStateStopped(true);
  }
}
