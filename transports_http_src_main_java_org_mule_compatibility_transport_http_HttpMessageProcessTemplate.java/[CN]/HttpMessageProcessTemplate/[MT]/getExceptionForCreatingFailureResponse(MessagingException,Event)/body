{
  MessagingException e=messagingException;
  if (response != null && response.getError().isPresent() && response.getError().get().getCause() instanceof MessagingException) {
    e=(MessagingException)response.getError().get().getCause();
  }
  return e;
}
