{
  MessagingException e=messagingException;
  if (response != null && response.getError() != null && response.getError().getException() instanceof MessagingException) {
    e=(MessagingException)response.getError().getException();
  }
  return e;
}
