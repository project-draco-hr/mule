{
  MessagingException e=messagingException;
  if (response != null && response.getError().isPresent() && response.getError().get().getException() instanceof MessagingException) {
    e=(MessagingException)response.getError().get().getException();
  }
  return e;
}
