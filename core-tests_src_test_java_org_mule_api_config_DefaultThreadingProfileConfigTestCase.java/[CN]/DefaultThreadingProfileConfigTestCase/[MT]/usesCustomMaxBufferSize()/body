{
  final int customValue=MAX_BUFFER_SIZE + 1;
  testWithSystemProperty(DefaultThreadingProfileConfig.MAX_BUFFER_SIZE_PROPERTY,Integer.toString(customValue),new MuleTestUtils.TestCallback(){
    @Override public void run() throws Exception {
      checkMaxBufferSize(customValue);
    }
  }
);
}
