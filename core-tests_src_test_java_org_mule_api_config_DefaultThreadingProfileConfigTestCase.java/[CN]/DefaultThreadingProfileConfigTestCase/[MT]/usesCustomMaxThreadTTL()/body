{
  final long customValue=MAX_THREAD_TTL + 1;
  testWithSystemProperty(DefaultThreadingProfileConfig.MAX_THREAD_TTL_PROPERTY,Long.toString(customValue),new MuleTestUtils.TestCallback(){
    @Override public void run() throws Exception {
      checkMaxThreadTTL(customValue);
    }
  }
);
}
