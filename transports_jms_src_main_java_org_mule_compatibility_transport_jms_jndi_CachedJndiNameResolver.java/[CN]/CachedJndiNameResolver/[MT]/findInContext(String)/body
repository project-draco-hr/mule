{
  Context jndiContext=createInitialContext();
  try {
    Object result=jndiContext.lookup(name);
    if (result != null) {
      cache.put(name,result);
    }
    return result;
  }
  finally {
    jndiContext.close();
  }
}
