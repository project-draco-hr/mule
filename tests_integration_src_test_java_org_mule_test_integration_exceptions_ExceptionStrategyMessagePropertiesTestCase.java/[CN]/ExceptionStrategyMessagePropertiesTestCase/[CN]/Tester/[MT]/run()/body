{
  try {
    for (int i=0; i < numMessages; ++i) {
      flowRunner("inbound").withPayload(getTestMuleMessage()).withInboundProperty("foo","bar").asynchronously().run();
    }
  }
 catch (  Exception e) {
    fail(e.getMessage());
  }
}
