{
  final HttpProxy httpProxy=new HttpProxyBuilder().name("test-http-proxy-no-cache").inboundAddress("test://foo").outboundAddress("test://bar").build(muleContext);
  assertEquals("test-http-proxy-no-cache",httpProxy.getName());
}
