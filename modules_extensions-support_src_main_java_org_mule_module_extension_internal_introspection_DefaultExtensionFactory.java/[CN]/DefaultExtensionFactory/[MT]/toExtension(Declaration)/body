{
  validateMuleVersion(declaration);
  return new ImmutableExtensionModel(declaration.getName(),declaration.getDescription(),declaration.getVersion(),sortConfigurations(toConfigurations(declaration.getConfigurations())),alphaSortDescribedList(toOperations(declaration.getOperations())),declaration.getCapabilities());
}
