{
  if (xstream == null) {
    if (useJaxpDom) {
      xstream=new XStream(new DomDriver());
    }
 else {
      xstream=new XStream(new XppDriver());
    }
  }
  addAliases();
  addConverters();
  return xstream;
}
