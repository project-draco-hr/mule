{
  flow=getTestFlow();
  messageContext=DefaultMessageContext.create(flow,TEST_CONNECTOR);
  muleEvent=MuleEvent.builder(messageContext).message(muleMessage).exchangePattern(REQUEST_RESPONSE).flow(flow).build();
}
