{
  UMOImmutableEndpoint ep=managementContext.getRegistry().lookupEndpointFactory().getInboundEndpoint("jetty:rest://localhost:8080/loanbroker",managementContext);
  assertNotNull(ep);
  assertTrue(ep.getConnector() instanceof JettyConnector);
}
