{
  if (NAME.equals(name)) {
    this.name=value;
    if (null != provider) {
      setName();
    }
  }
 else {
    super.addProperty(builder,name,value,reference);
    if (PROVIDERS.equals(name)) {
      provider=value;
      if (null != this.name) {
        setName();
      }
    }
  }
}
