{
  if (Thread.interrupted()) {
    throw new InterruptedException();
  }
synchronized (list) {
    while (list.isEmpty()) {
      list.wait();
    }
    Object o=list.removeFirst();
    list.notifyAll();
    return o;
  }
}
