{
  simpleService.initialise();
  simpleService.start();
  MuleEvent response=dims.process(MuleTestUtils.getTestEvent("hello",getAsynchronousTestInboundEndpoint(),muleContext));
  assertEquals("olleh",response.getMessageAsString());
}
