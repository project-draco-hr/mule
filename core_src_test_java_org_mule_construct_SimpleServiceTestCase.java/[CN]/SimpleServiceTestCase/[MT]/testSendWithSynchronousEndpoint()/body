{
  simpleService.initialise();
  simpleService.start();
  MuleEvent response=dims.process(MuleTestUtils.getTestEvent("hello",getSynchronousTestInboundEndpoint(),muleContext));
  assertEquals("olleh",response.getMessageAsString());
}
