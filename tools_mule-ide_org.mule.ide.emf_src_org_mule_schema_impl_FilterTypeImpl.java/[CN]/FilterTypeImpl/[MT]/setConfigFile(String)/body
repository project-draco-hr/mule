{
  String oldConfigFile=configFile;
  configFile=newConfigFile;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,SchemaPackage.FILTER_TYPE__CONFIG_FILE,oldConfigFile,configFile));
}
