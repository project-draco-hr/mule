{
synchronized (folderLock) {
    if (null != folder) {
      folder.removeMessageCountListener(this);
      try {
        closeFolder();
      }
 catch (      Exception e) {
        handleFolderException("closing",e);
      }
    }
  }
}
