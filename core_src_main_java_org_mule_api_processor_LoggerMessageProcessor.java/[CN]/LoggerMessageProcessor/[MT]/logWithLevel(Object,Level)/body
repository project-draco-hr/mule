{
switch (level.toInt()) {
case Level.ERROR_INT:
    logger.error(object);
case Level.WARN_INT:
  logger.warn(object);
case Level.INFO_INT:
logger.info(object);
case Level.DEBUG_INT:
logger.debug(object);
case Level.TRACE_INT:
logger.trace(object);
}
}
