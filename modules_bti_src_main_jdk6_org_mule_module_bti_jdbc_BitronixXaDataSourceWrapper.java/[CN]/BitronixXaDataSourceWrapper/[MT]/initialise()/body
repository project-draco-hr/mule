{
  if (!(muleContext.getTransactionManager() instanceof TransactionManagerWrapper)) {
    throw new InitialisationException(MessageFactory.createStaticMessage("Cannot use a Bitronix data source " + "pool without using Bitronix transaction manager"),this);
  }
}
