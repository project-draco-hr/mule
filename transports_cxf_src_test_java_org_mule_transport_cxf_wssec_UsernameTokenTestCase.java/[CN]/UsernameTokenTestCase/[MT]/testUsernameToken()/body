{
  GreeterImpl impl=getGreeter();
  int i=0;
  while (i++ < 1000) {
    if (impl.getInvocationCount() > 0) {
      break;
    }
    Thread.sleep(50);
  }
  assertEquals(1,impl.getInvocationCount());
}
