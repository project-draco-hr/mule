{
  if (isStartable) {
    try {
      ((Startable)component).start();
    }
 catch (    Exception e) {
      throw new MuleException("Failed to start Mule UMO: " + descriptor.getName(),e);
    }
  }
  started=true;
}
