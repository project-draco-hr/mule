{
  if (isDisposable) {
    try {
      ((Disposable)component).dispose();
    }
 catch (    Exception e) {
      throw new MuleException("Failed to dispose Mule UMO: " + descriptor.getName(),e);
    }
  }
  disposed=true;
}
