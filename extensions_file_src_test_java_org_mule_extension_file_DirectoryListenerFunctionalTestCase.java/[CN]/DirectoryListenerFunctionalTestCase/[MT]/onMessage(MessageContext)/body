{
  Object payload=messageContext.getPayload() != null ? messageContext.getPayload() : NullPayload.getInstance();
  MuleMessage message=MuleMessage.builder().payload(payload).mediaType(messageContext.getDataType().getMediaType()).attributes(messageContext.getAttributes()).build();
  receivedMessages.add(message);
}
