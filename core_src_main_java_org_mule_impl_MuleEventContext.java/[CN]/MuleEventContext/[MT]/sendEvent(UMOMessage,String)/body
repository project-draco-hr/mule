{
  UMOImmutableEndpoint endpoint=RegistryContext.getRegistry().lookupOutboundEndpoint(endpointName,getManagementContext());
  setRemoteSync(message,endpoint);
  return session.sendEvent(message,endpoint);
}
