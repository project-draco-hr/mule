{
  UMOImmutableEndpoint endpoint=getManagementContext().getRegistry().createEndpoint(endpointUri,UMOEndpoint.ENDPOINT_TYPE_SENDER,getManagementContext());
  setRemoteSync(message,endpoint);
  return session.sendEvent(message,endpoint);
}
