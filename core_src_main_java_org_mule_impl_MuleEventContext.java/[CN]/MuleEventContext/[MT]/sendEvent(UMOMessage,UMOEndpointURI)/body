{
  UMOImmutableEndpoint endpoint=getManagementContext().getRegistry().lookupEndpointFactory().getEndpoint(endpointUri,UMOImmutableEndpoint.ENDPOINT_TYPE_SENDER,getManagementContext());
  setRemoteSync(message,endpoint);
  return session.sendEvent(message,endpoint);
}
