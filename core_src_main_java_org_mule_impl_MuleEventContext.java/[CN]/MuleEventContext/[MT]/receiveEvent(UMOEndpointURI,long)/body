{
  UMOImmutableEndpoint endpoint=getMuleContext().getRegistry().lookupEndpointFactory().getEndpoint(endpointUri,UMOImmutableEndpoint.ENDPOINT_TYPE_SENDER);
  return session.receiveEvent(endpoint,timeout);
}
