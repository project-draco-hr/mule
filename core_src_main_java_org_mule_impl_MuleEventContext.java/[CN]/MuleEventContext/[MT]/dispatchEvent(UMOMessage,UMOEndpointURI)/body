{
  UMOImmutableEndpoint endpoint=getManagementContext().getRegistry().lookupEndpointFactory().getEndpoint(endpointUri,UMOEndpoint.ENDPOINT_TYPE_SENDER,getManagementContext());
  session.dispatchEvent(message,endpoint);
}
