{
  mvel.setAliases(Collections.singletonMap("foo","'bar'"));
  Event event=getTestEvent("");
  event=Event.builder(event).addVariable("foo","other").build();
  mvel.initialise();
  assertEquals("bar",evaluate("foo"));
}
