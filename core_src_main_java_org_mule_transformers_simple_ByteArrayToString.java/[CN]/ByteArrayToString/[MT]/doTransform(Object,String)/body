{
  if (src instanceof String) {
    return src;
  }
 else {
    try {
      return new String((byte[])src,encoding);
    }
 catch (    UnsupportedEncodingException e) {
      throw new TransformerException(MessageFactory.createStaticMessage("Unable to convert byte[] to String."),e);
    }
  }
}
