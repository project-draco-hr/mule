{
  super.doInitialise();
  try {
    if (dataSource == null) {
      initJndiContext();
      createDataSource();
    }
  }
 catch (  Exception e) {
    throw new InitialisationException("Failed to create Jdbc Connector: " + e.getMessage(),e);
  }
}
