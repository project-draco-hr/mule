{
  return new SpringXmlConfigurationMuleArtifactFactory(){
    @Override protected void addBuilders(    List<ConfigurationBuilder> builders){
      super.addBuilders(builders);
      builders.add(testServicesConfigurationBuilder);
    }
  }
;
}
