{
  ModelServiceDescriptor sd=(ModelServiceDescriptor)RegistryContext.getRegistry().lookupServiceDescriptor(ServiceDescriptorFactory.MODEL_SERVICE_TYPE,type,null);
  if (sd != null) {
    UMOModel model=sd.createModel();
    model.setName(name);
    return model;
  }
 else   return null;
}
