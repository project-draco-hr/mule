{
  logger.info("Received deregistration of " + component.getType() + "/"+ component.getId());
  Registration ref=(Registration)store.get(component.getId());
  if (ref == null)   return;
  store.remove(ref);
}
