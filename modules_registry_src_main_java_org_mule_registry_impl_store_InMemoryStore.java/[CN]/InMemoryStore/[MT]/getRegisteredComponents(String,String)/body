{
  Map components=new HashMap();
  Iterator iter=store.keySet().iterator();
  while (iter.hasNext()) {
    String id=(String)iter.next();
    Registration ref=(Registration)store.get(id);
    if (ref.getType().equals(type))     store.put(ref.getId(),ref);
  }
  return components;
}
