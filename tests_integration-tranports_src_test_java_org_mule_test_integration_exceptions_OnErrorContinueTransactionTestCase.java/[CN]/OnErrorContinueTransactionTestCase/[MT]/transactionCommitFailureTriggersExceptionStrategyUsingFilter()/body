{
  final InternalMessage muleMessage=InternalMessage.builder().payload(TEST_PAYLOAD).addOutboundProperty("filterMessage",true).build();
  transactionCommitFailureExecutesExceptionStrategy(muleMessage);
}
