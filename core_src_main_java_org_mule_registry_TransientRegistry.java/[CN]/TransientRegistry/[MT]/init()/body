{
  registry=new HashMap(8);
  getObjectTypeMap(ObjectProcessor.class).put("_mulePropertyExtractorProcessor",new PropertyExtractorProcessor());
  RegistryContext.setRegistry(this);
  try {
    initialise();
  }
 catch (  InitialisationException e) {
    logger.error(e);
  }
}
