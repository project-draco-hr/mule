{
  Flow flow;
  try {
    flow=getTestFlow();
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  return MuleEvent.builder(DefaultMessageContext.create(flow,TEST_CONNECTOR)).message(MuleMessage.builder().payload(payload).build()).exchangePattern(ONE_WAY).flow(flow).build();
}
