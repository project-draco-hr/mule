{
  return flowRunner(name).withPayload(resourceAsString("request/" + bodyRequestFileName)).withVariable(HEADER_IN,resourceAsString("request/" + HEADER_IN_XML)).withVariable(HEADER_INOUT,resourceAsString("request/" + HEADER_INOUT_XML)).run().getMessage();
}
