{
  if (asynchronous) {
    AsynchronousEventListener aListener=new AsynchronousEventListener((((MuleDescriptor)descriptor).getThreadingProfile()).createPool("spring-events"),listener);
    listeners.add(aListener);
  }
 else {
    listeners.add(listener);
  }
}
