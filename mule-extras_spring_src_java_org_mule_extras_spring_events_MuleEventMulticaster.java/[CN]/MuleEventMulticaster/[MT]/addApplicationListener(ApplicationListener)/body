{
  if (asynchronous) {
    AsynchronousEventListener aListener=new AsynchronousEventListener(MuleManager.getConfiguration().getDefaultThreadingProfile().createPool("spring-events"),listener);
    listeners.add(aListener);
  }
 else {
    listeners.add(listener);
  }
}
