{
  if (isBlank(basePath)) {
    try {
      return client.getWorkingDirectory();
    }
 catch (    Exception e) {
      throw new MuleRuntimeException(createStaticMessage("SFTP working dir was not specified and failed to resolve a default one"),e);
    }
  }
  return basePath;
}
