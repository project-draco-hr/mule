{
  sendOperation.send(connection,content,subject,toAddresses,configuration.getFrom(),ccAddresses != null ? ccAddresses : new ArrayList<>(),bccAddresses != null ? bccAddresses : new ArrayList<>(),headers != null ? headers : new HashMap<>(),attachments);
}
