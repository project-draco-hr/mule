{
  if (dispatcherWorkManager == null) {
    dispatcherWorkManager=this.getDispatcherThreadingProfile().createWorkManager(getName() + ".dispatcher");
    dispatcherWorkManager.start();
  }
  return dispatcherWorkManager;
}
