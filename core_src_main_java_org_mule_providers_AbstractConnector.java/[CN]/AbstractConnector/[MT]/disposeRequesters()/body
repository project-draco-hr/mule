{
  if (requesters != null) {
    logger.debug("Disposing Requesters");
    try {
      if (this.isDisposing()) {
        requesters.close();
      }
 else {
        requesters.clear();
      }
    }
 catch (    Exception ex) {
    }
    logger.debug("Requesters Disposed");
  }
}
