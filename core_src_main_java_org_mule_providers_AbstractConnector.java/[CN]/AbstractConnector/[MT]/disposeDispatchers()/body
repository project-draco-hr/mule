{
  if (dispatchers != null) {
    logger.debug("Disposing Dispatchers");
    try {
      if (this.isDisposing()) {
        dispatchers.close();
      }
 else {
        dispatchers.clear();
      }
    }
 catch (    Exception ex) {
    }
    logger.debug("Dispatchers Disposed");
  }
}
