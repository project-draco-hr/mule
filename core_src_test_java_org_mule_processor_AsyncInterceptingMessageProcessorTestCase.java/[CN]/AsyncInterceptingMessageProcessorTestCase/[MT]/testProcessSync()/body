{
  MuleEvent event=getTestEvent(TEST_MESSAGE,getTestOutboundEndpoint("","test://test?exchangePattern=request-response"));
  MuleEvent result=messageProcessor.process(event);
  assertSame(event,target.sensedEvent);
  assertSame(event,result);
}
