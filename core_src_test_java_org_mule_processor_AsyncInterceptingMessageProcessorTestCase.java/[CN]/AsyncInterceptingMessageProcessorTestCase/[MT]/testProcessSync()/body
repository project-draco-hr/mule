{
  MuleEvent event=getTestEvent(TEST_MESSAGE,getTestOutboundEndpoint("","test://test?synchronous=true"));
  MuleEvent result=messageProcessor.process(event);
  assertSame(event,target.sensedEvent);
  assertSame(event,result);
}
