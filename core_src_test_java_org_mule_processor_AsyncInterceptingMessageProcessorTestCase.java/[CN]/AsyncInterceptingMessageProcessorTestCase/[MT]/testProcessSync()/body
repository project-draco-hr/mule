{
  MuleEvent event=getTestEvent(TEST_MESSAGE,getTestOutboundEndpoint("","test://test?exchange-pattern=request-response"));
  MuleEvent result=messageProcessor.process(event);
  assertSame(event,target.sensedEvent);
  assertSame(event,result);
}
