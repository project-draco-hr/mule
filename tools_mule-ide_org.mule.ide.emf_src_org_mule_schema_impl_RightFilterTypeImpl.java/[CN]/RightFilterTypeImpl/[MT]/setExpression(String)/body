{
  String oldExpression=expression;
  expression=newExpression;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,SchemaPackage.RIGHT_FILTER_TYPE__EXPRESSION,oldExpression,expression));
}
