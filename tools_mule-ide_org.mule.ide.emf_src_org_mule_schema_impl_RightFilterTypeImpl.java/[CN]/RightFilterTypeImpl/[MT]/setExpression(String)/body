{
  String oldExpression=expression;
  expression=newExpression;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,MulePackage.RIGHT_FILTER_TYPE__EXPRESSION,oldExpression,expression));
}
