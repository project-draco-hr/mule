{
  String oldConfigFile=configFile;
  configFile=newConfigFile;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,SchemaPackage.RIGHT_FILTER_TYPE__CONFIG_FILE,oldConfigFile,configFile));
}
