{
  String oldExpectedType=expectedType;
  expectedType=newExpectedType;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,MulePackage.RIGHT_FILTER_TYPE__EXPECTED_TYPE,oldExpectedType,expectedType));
}
