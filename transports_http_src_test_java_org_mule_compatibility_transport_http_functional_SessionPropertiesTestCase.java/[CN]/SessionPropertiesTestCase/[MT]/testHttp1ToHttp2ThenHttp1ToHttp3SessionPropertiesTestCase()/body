{
  MuleClient client=muleContext.getClient();
  Map<String,Serializable> properties=Collections.emptyMap();
  InternalMessage response=client.send("http://localhost:" + dynamicPort4.getNumber() + "/Flow1s2","some message",properties).getRight();
  assertNotNullAndNotExceptionResponse(response);
}
