{
  Flow flow;
  try {
    flow=getTestFlow();
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  return new DefaultMuleEvent(DefaultMessageContext.create(flow,TEST_CONNECTOR),MuleMessage.builder().payload(payload).build(),ONE_WAY,flow);
}
