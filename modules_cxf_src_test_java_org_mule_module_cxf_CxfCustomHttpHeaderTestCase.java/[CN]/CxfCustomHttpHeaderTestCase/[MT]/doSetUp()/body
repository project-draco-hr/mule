{
  latch=new CountDownLatch(1);
  muleContext.registerListener(this);
  MuleClient client=new MuleClient(muleContext);
  endpointAddress=((InboundEndpoint)client.getMuleContext().getRegistry().lookupObject("cxfInbound")).getAddress() + "?method=onReceive";
}
