{
  MuleClient client=new MuleClient("axis-client-endpoint-config.xml");
  MuleMessage result=client.send("calculatorAddEndpoint",new Object[]{new Integer(3),new Integer(3)},null);
  assertNotNull(result);
  assertEquals(result.getPayload(),new Integer(6));
}
