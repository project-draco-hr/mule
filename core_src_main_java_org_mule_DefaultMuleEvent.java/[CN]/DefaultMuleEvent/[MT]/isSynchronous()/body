{
  if (endpoint instanceof OutboundEndpoint) {
    return endpoint.isSynchronous();
  }
 else {
    if (synchronous != null) {
      return synchronous.booleanValue();
    }
 else {
      return endpoint.isSynchronous();
    }
  }
}
