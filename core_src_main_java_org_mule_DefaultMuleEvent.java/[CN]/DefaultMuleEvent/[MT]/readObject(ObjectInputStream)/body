{
  in.defaultReadObject();
  int endpointHashCode=in.readInt();
  endpoint=(ImmutableEndpoint)MuleServer.getMuleContext().getRegistry().lookupObject(DefaultEndpointFactory.ENDPOINT_REGISTRY_PREFIX + endpointHashCode);
}
