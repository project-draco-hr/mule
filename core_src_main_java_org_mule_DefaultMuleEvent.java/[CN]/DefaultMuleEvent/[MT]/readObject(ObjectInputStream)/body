{
  logger=LogFactory.getLog(getClass());
  in.defaultReadObject();
  int hashCode=in.readInt();
  endpoint=MuleServer.getMuleContext().getRegistry().lookupEndpoint(DefaultEndpointFactory.ENDPOINT_REGISTRY_PREFIX + hashCode);
}
