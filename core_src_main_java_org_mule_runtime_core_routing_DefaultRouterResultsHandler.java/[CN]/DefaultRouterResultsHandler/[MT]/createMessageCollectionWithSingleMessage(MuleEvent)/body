{
  final MuleMessage coll=MuleMessage.builder().collectionPayload(singletonList(event.getMessage()),MuleMessage.class).build();
  event.setMessage(coll);
  return OptimizedRequestContext.unsafeSetEvent(event);
}
