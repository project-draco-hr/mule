{
  if (obj instanceof ParameterWrapper) {
    ParameterWrapper other=(ParameterWrapper)obj;
    return parameter.getName().equals(other.parameter.getName()) && getType(parameter.getType()).equals(getType(other.parameter.getType()));
  }
  return false;
}
