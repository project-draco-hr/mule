{
  event.setEndpointFields(endpoint.getEncoding(),endpoint.getExchangePattern(),endpoint.getName(),endpoint.getEndpointURI().getUri(),endpoint.getTransactionConfig().isTransacted());
  fillProperties(event,endpoint);
}
