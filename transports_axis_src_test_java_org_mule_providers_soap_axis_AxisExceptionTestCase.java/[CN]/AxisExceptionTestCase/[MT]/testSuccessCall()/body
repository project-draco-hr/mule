{
  MuleClient client=new MuleClient();
  UMOMessage reply=client.send("axis:http://localhost:63381/services/AxisService?method=receive",new MuleMessage("test"));
  assertNotNull(reply);
  assertNotNull(reply.getPayload());
  assertTrue(reply.getPayload() instanceof String);
  assertEquals("Received: test",reply.getPayloadAsString());
}
