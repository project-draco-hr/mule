{
  if (next == null) {
    return event;
  }
 else   if (event.getEndpoint().isSynchronous() || event.getEndpoint().getTransactionConfig().isTransacted()) {
    return next.process(event);
  }
 else {
    processAsync(event);
    return null;
  }
}
