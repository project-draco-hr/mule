{
  if (event.isSynchronous() || event.isTransacted()) {
    throw new MessagingException(CoreMessages.createStaticMessage("Unable to process a synchonrous event asyncronously"),event,this);
  }
  return doThreading && !event.isSynchronous();
}
