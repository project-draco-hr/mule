{
  InternalMessage muleMessage=InternalMessage.builder().payload(TEST_PAYLOAD).build();
  assertDataType(InternalMessage.builder(muleMessage).payload(1).mediaType(APPLICATION_XML_CUSTOM).build(),Integer.class,APPLICATION_XML,CUSTOM_ENCODING);
}
