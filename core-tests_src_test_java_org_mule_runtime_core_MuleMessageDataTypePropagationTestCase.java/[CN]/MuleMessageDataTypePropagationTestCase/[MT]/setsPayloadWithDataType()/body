{
  MuleMessage muleMessage=MuleMessage.builder().payload(TEST).build();
  assertDataType(MuleMessage.builder(muleMessage).payload(1).mediaType(APPLICATION_XML_CUSTOM).build(),Integer.class,APPLICATION_XML,CUSTOM_ENCODING);
}
