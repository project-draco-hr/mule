{
  MuleMessage muleMessage=MuleMessage.builder().payload(TEST).build();
  assertDataType(MuleMessage.builder(muleMessage).payload(NullPayload.getInstance()).mediaType(APPLICATION_XML_CUSTOM).build(),Object.class,APPLICATION_XML,CUSTOM_ENCODING);
}
