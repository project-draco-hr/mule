{
  MuleMessage muleMessage=MuleMessage.builder().payload(TEST).build();
  assertDataType(MuleMessage.builder(muleMessage).nullPayload().mediaType(APPLICATION_XML_CUSTOM).build(),Object.class,APPLICATION_XML,CUSTOM_ENCODING);
}
