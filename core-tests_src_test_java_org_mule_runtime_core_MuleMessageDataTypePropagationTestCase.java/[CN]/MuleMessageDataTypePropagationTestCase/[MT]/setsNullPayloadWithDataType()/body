{
  InternalMessage muleMessage=InternalMessage.builder().payload(TEST).build();
  assertDataType(InternalMessage.builder(muleMessage).nullPayload().mediaType(APPLICATION_XML_CUSTOM).build(),Object.class,APPLICATION_XML,CUSTOM_ENCODING);
}
