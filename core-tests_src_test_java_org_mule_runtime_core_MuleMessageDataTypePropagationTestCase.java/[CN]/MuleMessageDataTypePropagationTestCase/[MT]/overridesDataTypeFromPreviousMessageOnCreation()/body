{
  MuleMessage message=MuleMessage.builder().payload(1).mediaType(APPLICATION_XML_CUSTOM).build();
  assertDataType(MuleMessage.builder(message).payload("TEST").build(),String.class,APPLICATION_XML,CUSTOM_ENCODING);
}
