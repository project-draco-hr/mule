{
  try {
    return super.processStatement(connection,statement,queryTemplate,autoGeneratedKeyStrategy);
  }
  finally {
    try {
      statement.close();
    }
  finally {
      connection.release();
    }
  }
}
