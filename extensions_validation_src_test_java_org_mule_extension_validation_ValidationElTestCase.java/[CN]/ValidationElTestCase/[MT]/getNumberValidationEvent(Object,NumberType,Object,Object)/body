{
  Event event=Event.builder(eventBuilder().message(InternalMessage.of(value)).build()).addVariable("numberType",numberType).addVariable("minValue",minValue).addVariable("maxValue",maxValue).build();
  return event;
}
