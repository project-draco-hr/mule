{
  try {
    event.setMessage(MuleMessage.builder(event.getMessage()).addOutboundAttachment(key,IOUtils.toDataHandler(key,object,contentType)).build());
  }
 catch (  Exception e) {
    throw new MuleRuntimeException(e);
  }
}
