{
  try {
    org.w3c.dom.Document x3cDoc=(org.w3c.dom.Document)src;
    org.dom4j.Document dom4jDoc=new DOMReader().read(x3cDoc);
    return dom4jDoc.asXML();
  }
 catch (  Exception e) {
    throw new TransformerException("Failed to create XML String: " + e,e);
  }
}
