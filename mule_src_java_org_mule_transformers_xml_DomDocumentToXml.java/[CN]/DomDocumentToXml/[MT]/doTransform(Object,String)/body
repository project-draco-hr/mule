{
  try {
    if (byte[].class.equals(returnClass))     return convertToBytes(src,encoding);
 else     return convertToText(src);
  }
 catch (  Exception e) {
    throw new TransformerException(this,e);
  }
}
