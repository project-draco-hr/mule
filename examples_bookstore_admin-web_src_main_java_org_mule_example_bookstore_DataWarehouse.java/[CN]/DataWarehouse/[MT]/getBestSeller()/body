{
  String title="";
  int quantity=0;
  for (  Entry<String,Integer> entry : sales.entrySet()) {
    if (entry.getValue() > quantity) {
      title=entry.getKey();
      quantity=entry.getValue();
    }
  }
  return title;
}
