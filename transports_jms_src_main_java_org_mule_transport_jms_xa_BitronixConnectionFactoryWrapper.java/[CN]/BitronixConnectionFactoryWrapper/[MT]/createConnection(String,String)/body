{
  Connection xac=factory.createConnection(username,password);
  Connection proxy=(Connection)Proxy.newProxyInstance(Connection.class.getClassLoader(),new Class[]{Connection.class},new BitronixConnectionInvocationHandler(xac));
  return proxy;
}
