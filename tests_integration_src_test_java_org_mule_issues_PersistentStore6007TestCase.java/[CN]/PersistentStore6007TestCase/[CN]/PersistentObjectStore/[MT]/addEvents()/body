{
  for (  String str : new String[]{"A","B","C"}) {
    Flow flow=getTestFlow();
    MuleEvent event=new DefaultMuleEvent(create(flow),MuleMessage.builder().payload(str).build(),ONE_WAY,flow,new DefaultMuleSession());
    events.put(buildQueueKey(event,getTestFlow(),muleContext),event);
  }
}
