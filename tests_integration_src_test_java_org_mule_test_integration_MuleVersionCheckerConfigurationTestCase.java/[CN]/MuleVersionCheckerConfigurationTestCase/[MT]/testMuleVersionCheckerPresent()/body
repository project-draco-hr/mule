{
  MuleVersionChecker checker=muleContext.getRegistry().get(MuleProperties.MULE_VERSION_CHECKER);
  Assert.assertNotNull("was expecting to find checker under key " + MuleProperties.MULE_VERSION_CHECKER,checker);
}
