{
  if (event != null && event.getMessage() != null) {
    return MuleEvent.builder(event).message(MuleMessage.builder(event.getMessage()).payload(event.getMessage().getPayload() + ":" + label).build()).build();
  }
  return event;
}
