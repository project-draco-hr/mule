{
  ProcessInstance processInstance=processEngine.getExecutionService().startProcessInstanceByKey((String)processType,(Map)processVariables,(String)processInstanceKey);
  if (processInstance == null) {
    throw new IllegalArgumentException("No process definition found for process " + processType);
  }
  return processInstance;
}
