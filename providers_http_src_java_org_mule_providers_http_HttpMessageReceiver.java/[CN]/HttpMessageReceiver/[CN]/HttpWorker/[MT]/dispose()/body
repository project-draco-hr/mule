{
  try {
    if (socket != null) {
      logger.info("Closing listener: " + socket.getLocalAddress());
      socket.close();
    }
  }
 catch (  IOException e) {
    logger.error("Socket close failed with: " + e);
  }
  socket=null;
}
