{
  T baseStore;
  if (persistent) {
    baseStore=(T)muleContext.getRegistry().lookupObject(MuleProperties.DEFAULT_USER_PERSISTENT_OBJECT_STORE_NAME);
  }
 else {
    baseStore=(T)muleContext.getRegistry().lookupObject(MuleProperties.DEFAULT_USER_OBJECT_STORE_NAME);
  }
  return baseStore;
}
