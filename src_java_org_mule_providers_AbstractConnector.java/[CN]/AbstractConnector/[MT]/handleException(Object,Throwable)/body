{
  if (exceptionStrategy == null) {
    throw new MuleRuntimeException("Exception occurred in endpoint: " + getName() + ". Exception handler is not set. Message is: "+ message,exception);
  }
 else {
    exceptionStrategy.handleException(message,exception);
  }
}
