{
  if (initialised.get()) {
    throw new AlreadyInitialisedException("Connector: " + getName() + " is already initialised");
  }
  if (logger.isInfoEnabled())   logger.info("Initialising " + getClass().getName());
  doInitialise();
  initialised.set(true);
}
