{
  try {
    event.setMessage(MuleMessage.builder(event.getMessage()).payload(muleContext.getTransformationService().transform(event.getMessage(),DataType.STRING).getPayload() + letter).build());
  }
 catch (  Exception e) {
  }
  throw new DefaultMuleException(CoreMessages.createStaticMessage(""));
}
