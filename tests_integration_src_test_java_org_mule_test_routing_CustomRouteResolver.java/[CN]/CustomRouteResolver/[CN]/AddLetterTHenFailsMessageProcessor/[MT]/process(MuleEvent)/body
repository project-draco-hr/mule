{
  try {
    event.setMessage(new DefaultMuleMessage(event.getMuleContext().getTransformationService().transform(event.getMessage(),DataTypeFactory.STRING).getPayload() + letter,event.getMessage(),event.getMuleContext()));
  }
 catch (  Exception e) {
  }
  throw new DefaultMuleException(CoreMessages.createStaticMessage(""));
}
