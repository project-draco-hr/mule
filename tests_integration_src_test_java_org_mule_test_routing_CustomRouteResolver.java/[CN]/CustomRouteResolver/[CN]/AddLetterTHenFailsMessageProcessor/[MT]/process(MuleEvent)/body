{
  try {
    event.setMessage(new DefaultMuleMessage(event.getMuleContext().getTransformationService().transform(event.getMessage(),DataType.STRING).getPayload() + letter,event.getMessage()));
  }
 catch (  Exception e) {
  }
  throw new DefaultMuleException(CoreMessages.createStaticMessage(""));
}
