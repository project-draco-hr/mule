{
  for (  ExpressionLanguagePerEvaluationExtension extension : muleContext.getRegistry().lookupObjectsForLifecycle(ExpressionLanguagePerEvaluationExtension.class)) {
    extension.configureContext(context);
  }
}
