{
  Object value=null;
  for (  PropertyScope scope : SCOPE_ORDER) {
    Map props=(Map)scopedMap.get(scope);
    value=props.get(key);
    if (value != null) {
      break;
    }
  }
  return value;
}
