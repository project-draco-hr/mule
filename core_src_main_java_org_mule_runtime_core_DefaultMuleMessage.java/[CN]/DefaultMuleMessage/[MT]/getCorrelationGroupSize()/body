{
  assertAccess(READ);
  Object correlationGroupSize=findPropertyInSpecifiedScopes(MuleProperties.MULE_CORRELATION_GROUP_SIZE_PROPERTY,PropertyScope.OUTBOUND,INBOUND);
  return ObjectUtils.getInt(correlationGroupSize,-1);
}
