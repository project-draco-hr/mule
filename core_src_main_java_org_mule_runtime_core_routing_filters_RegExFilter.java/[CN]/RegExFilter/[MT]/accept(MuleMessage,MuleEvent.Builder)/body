{
  Flow flowConstruct=new Flow("",muleContext);
  return accept(MuleEvent.builder(create(flowConstruct,"RegExFilter")).message(message).exchangePattern(ONE_WAY).flow(flowConstruct).build(),builder);
}
