{
  try {
    event.getMessage().setPayload(getPayloadAsString(event.getMessage()) + name + BEFORE);
  }
 catch (  Exception e) {
    e.printStackTrace();
    fail(e.getMessage());
  }
  return event;
}
