{
  try {
    event.getMessage().setPayload(getPayloadAsString(event.getMessage()) + name + AFTER);
  }
 catch (  Exception e) {
    e.printStackTrace();
    fail(e.getMessage());
  }
  return event;
}
