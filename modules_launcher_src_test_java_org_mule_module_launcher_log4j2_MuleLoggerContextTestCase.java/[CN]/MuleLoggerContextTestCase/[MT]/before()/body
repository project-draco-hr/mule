{
  context=getDefaultContext();
  Appender appender=new TestAppender(TEST_APPENDER,null,null);
  context.getConfiguration().addAppender(appender);
  LoggerConfig loggerConfig=AsyncLoggerConfig.createLogger("false",LEVEL.name(),CATEGORY,"true",new AppenderRef[]{AppenderRef.createAppenderRef(TEST_APPENDER,null,null)},null,context.getConfiguration(),null);
  loggerConfig.addAppender(appender,null,null);
  loggerConfig.start();
  context.getConfiguration().addLogger(CATEGORY,loggerConfig);
  context.updateLoggers();
}
