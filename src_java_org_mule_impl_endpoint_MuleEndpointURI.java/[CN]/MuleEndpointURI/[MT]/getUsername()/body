{
  if (userInfo != null && !"".equals(userInfo)) {
    int i=userInfo.indexOf(":");
    if (i == -1) {
      return userInfo;
    }
 else {
      return userInfo.substring(0,i);
    }
  }
  return null;
}
