{
  StreamTransformer transformer=new DecryptStreamTransformer(toBeDecrypted,publicKey,secretKey,password);
  this.encryptedMessage=new LazyTransformedInputStream(new TransformContinuouslyPolicy(),transformer);
}
