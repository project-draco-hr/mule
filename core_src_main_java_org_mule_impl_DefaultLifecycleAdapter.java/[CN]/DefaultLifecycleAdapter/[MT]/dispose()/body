{
  if (isDisposable) {
    try {
      ((Disposable)component).dispose();
    }
 catch (    Exception e) {
      logger.error("failed to dispose: " + descriptor.getName(),e);
    }
  }
  disposed=true;
}
