{
  if (isStartable) {
    try {
      ((Startable)component).start();
    }
 catch (    Exception e) {
      throw new MuleException(CoreMessages.failedToStart("UMO Component: " + descriptor.getName()),e);
    }
  }
  started=true;
}
