{
  if (isStoppable) {
    try {
      ((Stoppable)component).stop();
    }
 catch (    Exception e) {
      throw new MuleException(CoreMessages.failedToStop("UMO Component: " + component.getName()),e);
    }
  }
  started=false;
}
