{
  assertAlienWasInserted(doTest(new XmlContentBuilder(){
    @Override public Object build(    Connection connection) throws Exception {
      return OracleXmlType.createXmlType(connection,Alien.ET.getXml());
    }
  }
));
}
