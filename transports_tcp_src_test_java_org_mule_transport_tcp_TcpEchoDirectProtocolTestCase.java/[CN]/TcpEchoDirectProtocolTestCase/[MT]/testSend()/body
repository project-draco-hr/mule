{
  MuleClient client=muleContext.getClient();
  MuleMessage response=client.send(((InboundEndpoint)muleContext.getRegistry().lookupObject("inBounceTcpMMP")).getAddress(),TEST_MESSAGE,null);
  assertNotNull(response);
  assertEquals(TEST_MESSAGE,response.getPayload());
}
