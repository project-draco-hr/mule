{
  try {
    muleContext.getInjector().inject(executorDelegate);
  }
 catch (  MuleException e) {
    throw new InitialisationException(createStaticMessage("Could not perform dependency injection on operation class " + executorDelegate.getClass().getName()),e,this);
  }
  initialiseIfNeeded(executorDelegate);
}
