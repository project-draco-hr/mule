{
  InputStream xml=getClass().getResourceAsStream("validation1.xml");
  MuleClient client=muleContext.getClient();
  MuleMessage message=client.send("http://localhost:" + dynamicPort.getNumber() + "/extract",getTestMuleMessage(xml),httpOptions).getRight();
  assertThat(getPayloadAsString(message),equalTo("some"));
}
