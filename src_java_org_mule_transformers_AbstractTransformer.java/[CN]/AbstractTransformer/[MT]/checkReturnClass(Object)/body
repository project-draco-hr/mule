{
  if (returnClass != null) {
    if (!returnClass.isInstance(object)) {
      throw new TransformerException("The object transformed is of type: " + object.getClass().getName() + ", but the expected return type is: "+ returnClass.getName());
    }
  }
  logger.debug("The transformed object is of expected type. Type is: " + object.getClass().getName());
  return object;
}
