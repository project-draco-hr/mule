{
  SftpClient sftpClient=new SftpClient(host);
  sftpClient.setPort(port);
  try {
    sftpClient.login(user,password);
  }
 catch (  Exception e) {
    fail("Login failed: " + e);
  }
  return sftpClient;
}
