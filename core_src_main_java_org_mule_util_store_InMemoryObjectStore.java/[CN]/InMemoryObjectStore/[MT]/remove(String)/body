{
synchronized (store) {
    Map.Entry<?,?> entry=findEntry(id);
    if (entry != null) {
      Object removedObject=store.remove(entry.getKey());
      return (removedObject != null);
    }
  }
  return true;
}
