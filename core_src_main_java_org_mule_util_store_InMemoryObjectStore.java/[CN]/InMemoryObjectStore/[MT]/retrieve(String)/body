{
synchronized (store) {
    Map.Entry<?,?> entry=findEntry(id);
    if (entry != null) {
      StoredObject object=(StoredObject)entry.getValue();
      return object.getItem();
    }
  }
  return null;
}
