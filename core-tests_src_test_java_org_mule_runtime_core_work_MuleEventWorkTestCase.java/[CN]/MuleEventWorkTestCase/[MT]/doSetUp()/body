{
  super.doSetUp();
  originalEvent=getTestEvent("test");
  originalEvent.setMessage(MuleMessage.builder(originalEvent.getMessage()).addOutboundProperty("test","val").addOutboundProperty("test2","val2").build());
  OptimizedRequestContext.unsafeSetEvent(originalEvent);
}
