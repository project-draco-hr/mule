{
  super.doSetUp();
  originalEvent=eventBuilder().message(InternalMessage.builder().payload("test").addOutboundProperty("test","val").addOutboundProperty("test2","val2").build()).build();
  setCurrentEvent(originalEvent);
}
