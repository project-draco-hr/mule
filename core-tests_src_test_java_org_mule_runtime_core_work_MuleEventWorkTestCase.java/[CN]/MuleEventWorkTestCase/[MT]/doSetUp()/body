{
  super.doSetUp();
  originalEvent=getTestEvent("test");
  originalEvent=MuleEvent.builder(originalEvent).message(MuleMessage.builder(originalEvent.getMessage()).addOutboundProperty("test","val").addOutboundProperty("test2","val2").build()).build();
  setCurrentEvent(originalEvent);
}
