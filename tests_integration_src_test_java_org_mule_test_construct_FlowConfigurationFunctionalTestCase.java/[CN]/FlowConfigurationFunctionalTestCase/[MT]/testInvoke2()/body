{
  MuleMessage message=new DefaultMuleMessage("0",muleContext);
  message.setOutboundProperty("one","header1val");
  assertEquals("header1valrecieved",getPayloadAsString(muleContext.getClient().send("vm://invoke2-in",message)));
}
