{
  flowRunner("messagefiltersubflow").withPayload("0").asynchronously().run();
  InternalMessage message=muleContext.getClient().request("test://messagefiltersubflow-out",RECEIVE_TIMEOUT).getRight().get();
  assertNotNull(message);
}
