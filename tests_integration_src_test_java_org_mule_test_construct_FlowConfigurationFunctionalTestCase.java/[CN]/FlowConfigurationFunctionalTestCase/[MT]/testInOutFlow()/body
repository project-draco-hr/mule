{
  muleContext.getClient().send("vm://inout-in",new DefaultMuleMessage("0",muleContext));
  assertEquals("0",getPayloadAsString(muleContext.getClient().request("vm://inout-out",RECEIVE_TIMEOUT)));
}
