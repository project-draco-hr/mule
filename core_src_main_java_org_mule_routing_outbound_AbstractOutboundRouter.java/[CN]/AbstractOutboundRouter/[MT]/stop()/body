{
synchronized (routes) {
    for (    MessageProcessor processor : routes) {
      if (processor instanceof Stoppable) {
        ((Stoppable)processor).stop();
      }
    }
    started.set(false);
  }
}
