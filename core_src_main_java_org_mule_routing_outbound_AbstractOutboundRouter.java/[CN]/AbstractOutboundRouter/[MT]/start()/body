{
synchronized (routes) {
    for (    MessageProcessor processor : routes) {
      if (processor instanceof Startable) {
        ((Startable)processor).start();
      }
    }
    started.set(true);
  }
}
