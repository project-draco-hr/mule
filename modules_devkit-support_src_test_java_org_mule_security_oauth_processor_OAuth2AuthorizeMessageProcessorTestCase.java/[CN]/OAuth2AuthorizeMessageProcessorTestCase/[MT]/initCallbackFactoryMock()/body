{
  this.callbackFactory=Mockito.mock(HttpCallbackFactory.class);
  HttpCallback callback=Mockito.mock(HttpCallback.class);
  Mockito.when(callbackFactory.createCallback(Mockito.any(HttpCallbackAdapter.class),Mockito.any(String.class),Mockito.any(FetchAccessTokenMessageProcessor.class),Mockito.any(MessageProcessor.class),Mockito.any(MuleContext.class),Mockito.any(FlowConstruct.class))).thenReturn(callback);
  return callback;
}
