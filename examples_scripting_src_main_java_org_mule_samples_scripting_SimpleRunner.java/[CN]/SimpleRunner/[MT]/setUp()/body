{
  MuleServer server=new MuleServer(config);
  server.start(true);
  while (!serverIsStarted()) {
    try {
      Thread.sleep(500);
    }
 catch (    InterruptedException e) {
      e.printStackTrace();
      System.exit(0);
    }
  }
}
