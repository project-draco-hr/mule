{
  for (int i=0; i < 10; i++) {
    user.deliver(newMessage(testSession).to(singletonList(JUANI_EMAIL)).fromAddresses(ESTEBAN_EMAIL).cc(singletonList(ALE_EMAIL)).withContent(EMAIL_CONTENT).withSubject(EMAIL_SUBJECT).build());
  }
}
