{
  Enumeration urls=MuleCorePlugin.getDefault().getBundle().findEntries("/","mule-core.jar",false);
  IClasspathEntry[] entries=convertUrlsToClasspathEntries(urls);
  if (entries.length > 0) {
    return entries[0];
  }
 else {
    return null;
  }
}
