{
  return contentParameter.isPresent() ? Optional.of(MetadataDescriptorBuilder.typeDescriptor(contentParameter.get().getName()).withType(contentParameter.get().getType()).build()) : Optional.empty();
}
