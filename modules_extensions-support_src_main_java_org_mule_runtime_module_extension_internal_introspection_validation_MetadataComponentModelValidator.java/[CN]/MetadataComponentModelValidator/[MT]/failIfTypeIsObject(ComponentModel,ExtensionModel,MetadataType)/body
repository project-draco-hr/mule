{
  if (Object.class.equals(getType(type))) {
    String componentTypeName=getComponentModelTypeName(componentModel);
    throw new IllegalModelDefinitionException(format("Extension '%s' specifies a/an %s named '%s' with type '%s' as return type. Operations and Sources with " + "return type such as Object or Map (or a collection of any of those) must have defined a not null OutputTypeResolver",extensionModel.getName(),componentTypeName,componentModel.getName(),getId(type)));
  }
}
