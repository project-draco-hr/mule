{
  TreeNode node=(TreeNode)flowRunner("list").withVariable("path","../").withVariable("recursive",false).run().getMessage().getPayload().getValue();
  assertThat(node.getChilds().isEmpty(),is(false));
}
