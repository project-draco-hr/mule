{
  TreeNode node=(TreeNode)flowRunner("list").withFlowVariable("path","../").withFlowVariable("recursive",false).run().getMessage().getPayload().getValue();
  assertThat(node.getChilds().isEmpty(),is(false));
}
