{
  final File pluginFolderExpanded=new File(getContainerAppPluginsFolder(),separator + applicationPluginFileBuilder.getId());
  FileUtils.unzip(applicationPluginFileBuilder.getArtifactFile(),pluginFolderExpanded);
}
