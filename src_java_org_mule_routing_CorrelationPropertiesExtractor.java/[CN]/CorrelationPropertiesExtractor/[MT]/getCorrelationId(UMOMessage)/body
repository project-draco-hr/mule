{
  String id=message.getCorrelationId();
  try {
    if (id == null) {
      id=message.getUniqueId();
    }
  }
 catch (  UniqueIdNotSupportedException e) {
    id=new UUID().getUUID();
  }
  return id;
}
