{
  this.port=port;
  if (address == null) {
    this.hostName=EMPTY;
    this.hostAddress=EMPTY;
  }
 else {
    this.hostName=address.getHostName();
    this.hostAddress=address.getHostAddress();
  }
}
