{
  try {
    lifecycleManager.fireStopPhase(new LifecycleCallback<Component>(){
      @Override public void onTransition(      String phaseName,      Component object) throws MuleException {
        doStop();
      }
    }
);
  }
 catch (  MuleException e) {
    e.printStackTrace();
    throw e;
  }
}
