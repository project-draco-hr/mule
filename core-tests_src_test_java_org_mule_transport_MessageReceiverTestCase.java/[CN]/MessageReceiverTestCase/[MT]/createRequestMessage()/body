{
  MuleMessage request=Mockito.mock(MuleMessage.class);
  Mockito.when(request.getMuleContext()).thenReturn(muleContext);
  Mockito.when(request.getProperty(Mockito.anyString(),Mockito.any(PropertyScope.class),Mockito.eq(false))).thenReturn(Boolean.FALSE);
  return request;
}
