{
  flowRunner(CLIENT_COOKIES_DISABLED_FLOW).withPayload(TEST_MESSAGE).run();
  assertNoCookiesSent();
  flowRunner(CLIENT_COOKIES_DISABLED_FLOW).withPayload(TEST_MESSAGE).run();
  assertNoCookiesSent();
}
