{
  try {
    if (!isDone) {
      sendInputStreamChunk();
    }
 else {
      close();
      responseStatusCallback.responseSendSuccessfully();
      resume();
    }
  }
 catch (  IOException e) {
    failed(e);
  }
}
