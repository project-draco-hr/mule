{
  if (next != null) {
    String normalized=normalize(next);
    if (normalized != null) {
      String pack=getPackage(normalized);
      packageReference(pack);
    }
  }
 else   throw new IllegalArgumentException("Invalid class, parent=");
}
