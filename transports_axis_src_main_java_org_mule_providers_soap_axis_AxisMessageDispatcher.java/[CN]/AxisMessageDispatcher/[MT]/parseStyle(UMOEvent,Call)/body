{
  String style=event.getMessage().getStringProperty("style",null);
  if (style != null) {
    Style s=Style.getStyle(style);
    if (s == null) {
      throw new IllegalArgumentException(CoreMessages.valueIsInvalidFor(style,"style").toString());
    }
 else {
      call.setOperationStyle(s);
    }
  }
}
