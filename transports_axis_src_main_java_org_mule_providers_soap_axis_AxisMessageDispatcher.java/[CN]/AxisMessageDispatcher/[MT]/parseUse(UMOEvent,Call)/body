{
  String use=event.getMessage().getStringProperty("use",null);
  if (use != null) {
    Use u=Use.getUse(use);
    if (u == null) {
      throw new IllegalArgumentException(CoreMessages.valueIsInvalidFor(use,"use").toString());
    }
 else {
      call.setOperationUse(u);
    }
  }
}
