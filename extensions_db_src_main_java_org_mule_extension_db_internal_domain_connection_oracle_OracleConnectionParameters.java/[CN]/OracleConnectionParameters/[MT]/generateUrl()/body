{
  StringBuilder buf=new StringBuilder(JDBC_URL_PREFIX);
  buf.append(host);
  buf.append(":");
  buf.append(port);
  if (instance != null) {
    buf.append(":");
    buf.append(instance);
  }
  return buf.toString();
}
