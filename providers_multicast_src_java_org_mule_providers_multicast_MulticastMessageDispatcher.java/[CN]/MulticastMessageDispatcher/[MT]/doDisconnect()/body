{
  try {
    if (socket != null) {
      ((MulticastSocket)socket).leaveGroup(inetAddress);
    }
  }
 catch (  IOException e) {
    logger.error("Failed to leave group: " + inetAddress);
  }
  super.doDisconnect();
}
