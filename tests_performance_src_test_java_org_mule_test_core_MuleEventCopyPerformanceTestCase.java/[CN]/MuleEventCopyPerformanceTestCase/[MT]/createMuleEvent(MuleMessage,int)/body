{
  Flow flow;
  try {
    flow=getTestFlow();
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  final MuleEvent.Builder builder=MuleEvent.builder(DefaultMessageContext.create(flow,TEST_CONNECTOR)).message(message).exchangePattern(ONE_WAY).flow(flow);
  for (int i=1; i <= numProperties; i++) {
    builder.addFlowVariable("FlOwVaRiAbLeKeY" + i,"val");
  }
  return builder.build();
}
