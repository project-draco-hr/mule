{
  MuleEvent original=muleEventWith50Properties;
  for (int i=0; i < repetitions; i++) {
    events[i]=MuleEvent.builder(original).session(new DefaultMuleSession(original.getSession())).addFlowVariable("newKey","val").message(MuleMessage.builder(events[i].getMessage()).addInboundProperty("newKey","val").addOutboundProperty("newKey","val").build()).build();
  }
}
