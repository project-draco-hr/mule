{
  Flow flow;
  try {
    flow=getTestFlow();
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  final Event.Builder builder=Event.builder(DefaultEventContext.create(flow,TEST_CONNECTOR)).message(message).exchangePattern(ONE_WAY).flow(flow);
  for (int i=1; i <= numProperties; i++) {
    builder.addVariable("FlOwVaRiAbLeKeY" + i,"val");
  }
  return builder.build();
}
