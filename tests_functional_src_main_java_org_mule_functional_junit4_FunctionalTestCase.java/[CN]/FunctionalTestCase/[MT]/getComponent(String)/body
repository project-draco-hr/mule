{
  final FlowConstruct flowConstruct=muleContext.getRegistry().lookupObject(serviceName);
  if (flowConstruct != null) {
    return getComponent(flowConstruct);
  }
 else {
    throw new RegistrationException(I18nMessageFactory.createStaticMessage("Service " + serviceName + " not found in Registry"));
  }
}
