{
  PooledExecutor pool;
  if (maxBufferSize > 0) {
    pool=new DisposableThreadPool(new BoundedBuffer(maxBufferSize));
  }
 else {
    pool=new DisposableThreadPool();
  }
  configurePool(pool);
  return pool;
}
