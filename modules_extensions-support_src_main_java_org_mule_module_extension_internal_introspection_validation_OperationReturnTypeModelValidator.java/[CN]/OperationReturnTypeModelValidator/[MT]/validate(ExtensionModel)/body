{
  for (  OperationModel operationModel : model.getOperationModels()) {
    if (operationModel.getReturnType() == null) {
      throw new IllegalModelDefinitionException(String.format("Operation '%s' in Extension '%s' is missing a return type",operationModel.getName(),model.getName()));
    }
  }
}
