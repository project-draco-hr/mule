{
  StringBuilder buf=new StringBuilder();
  for (  List<String> set : attributeSets) {
    if (buf.length() > 0) {
      buf.append(" ");
    }
    if (set.isEmpty()) {
      continue;
    }
    buf.append(set.toString());
  }
  return buf.toString();
}
