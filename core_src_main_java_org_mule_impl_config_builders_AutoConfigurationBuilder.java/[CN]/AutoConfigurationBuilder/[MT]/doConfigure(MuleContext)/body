{
  int count=0;
  for (int i=0; i < configResources.length; i++) {
    if (autoConfigure(muleContext,configResources[i])) {
      count++;
    }
  }
  logger.info("Configured Mule using AutoConfigurationBuilder with " + count + " of "+ configResources.length+ " configuration resouces,");
}
