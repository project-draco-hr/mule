{
  sendOperation.send(connection,content,subject,toAddresses,configuration.getFrom(),configuration.getDefaultCharset(),ensureNotNullList(ccAddresses),ensureNotNullList(bccAddresses),mergeHeaders(headers,configuration.getHeaders()),ensureNotNullList(attachments));
}
