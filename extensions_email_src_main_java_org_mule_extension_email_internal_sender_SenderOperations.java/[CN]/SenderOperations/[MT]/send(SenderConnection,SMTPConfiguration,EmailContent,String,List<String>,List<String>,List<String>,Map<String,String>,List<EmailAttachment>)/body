{
  sendOperation.send(connection,content,subject,toAddresses,configuration.getFrom(),configuration.getDefaultCharset(),ccAddresses != null ? ccAddresses : emptyList(),bccAddresses != null ? bccAddresses : emptyList(),mergeHeaders(headers,configuration.getHeaders()),attachments);
}
