{
  sendOperation.send(connection,content,subject,toAddresses,configuration.getFrom(),configuration.getDefaultCharset(),ccAddresses != null ? ccAddresses : new ArrayList<>(),bccAddresses != null ? bccAddresses : new ArrayList<>(),headers != null ? headers : new HashMap<>(),attachments);
}
