{
  forwardCommand.forward(connection,muleMessage,content,subject,configuration.getFrom(),configuration.getDefaultCharset(),toAddresses,ccAddresses,bccAddresses,mergeHeaders(headers,configuration.getHeaders()));
}
