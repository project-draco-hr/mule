{
  forwardCommand.forward(connection,muleMessage,content,subject,configuration.getFrom(),configuration.getDefaultCharset(),ensureNotNullList(toAddresses),ensureNotNullList(ccAddresses),ensureNotNullList(bccAddresses),mergeHeaders(headers,configuration.getHeaders()),ensureNotNullList(attachments));
}
