{
  replyOperation.reply(connection,muleMessage,content,subject,configuration.getFrom(),configuration.getDefaultCharset(),mergeHeaders(headers,configuration.getHeaders()),ensureNotNullList(attachments),replyToAll);
}
