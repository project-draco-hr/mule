{
  return workEvent.getSource() == source && workEvent.getWork() == work && workEvent.getException() == workException && workEvent.getStartDuration() == startDuration && workEvent.getType() == type;
}
