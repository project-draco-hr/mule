{
  if (!disposed) {
    try {
      try {
        disconnect();
      }
 catch (      Exception e) {
        logger.warn(e.getMessage(),e);
      }
      doDispose();
      if (workManager != null) {
        workManager.dispose();
      }
    }
  finally {
      disposed=true;
    }
  }
}
