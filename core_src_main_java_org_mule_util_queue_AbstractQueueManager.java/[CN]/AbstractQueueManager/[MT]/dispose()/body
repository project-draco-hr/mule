{
  queuesLock.lock();
  try {
    for (    CacheAwareQueueStore queueInfo : queues.values()) {
      queueInfo.getDelegate().dispose();
    }
  }
  finally {
    queuesLock.unlock();
  }
  doDispose();
}
