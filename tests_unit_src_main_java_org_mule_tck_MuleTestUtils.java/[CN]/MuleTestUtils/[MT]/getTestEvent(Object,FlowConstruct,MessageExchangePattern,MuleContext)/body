{
  final MuleSession session=getTestSession(flowConstruct,context);
  final DefaultMuleEvent event=new DefaultMuleEvent(new DefaultMuleMessage(data,DataType.builder().fromObject(data).charset(getDefaultEncoding(context)).build()),mep,flowConstruct,session);
  return event;
}
