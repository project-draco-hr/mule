{
  return Event.builder(DefaultEventContext.create(flowConstruct,TEST_CONNECTOR)).message(InternalMessage.builder().payload(data).build()).exchangePattern(mep).flow(flowConstruct).session(getTestSession(flowConstruct,context)).build();
}
