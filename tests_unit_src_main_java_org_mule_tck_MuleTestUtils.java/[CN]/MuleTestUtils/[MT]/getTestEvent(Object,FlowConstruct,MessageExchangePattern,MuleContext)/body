{
  return MuleEvent.builder(DefaultMessageContext.create(flowConstruct,TEST_CONNECTOR)).message(MuleMessage.builder().payload(data).build()).exchangePattern(mep).flow(flowConstruct).session(getTestSession(flowConstruct,context)).build();
}
