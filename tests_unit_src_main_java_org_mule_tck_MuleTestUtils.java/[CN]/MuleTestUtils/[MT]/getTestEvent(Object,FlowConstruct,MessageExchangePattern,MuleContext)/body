{
  final MuleSession session=getTestSession(flowConstruct,context);
  final DefaultMuleEvent event=new DefaultMuleEvent(create(flowConstruct),MuleMessage.builder().payload(data).build(),mep,flowConstruct,session);
  return event;
}
