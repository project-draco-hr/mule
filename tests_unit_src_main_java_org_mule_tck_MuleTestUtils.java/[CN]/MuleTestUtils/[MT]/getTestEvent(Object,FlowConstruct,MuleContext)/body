{
  final MuleSession session=getTestSession(flowConstruct,context);
  final DefaultMuleEvent event=new DefaultMuleEvent(DefaultMessageContext.create(flowConstruct,TEST_CONNECTOR),MuleMessage.builder().payload(data).build(),flowConstruct,session);
  return event;
}
