{
  String key=String.format("%s-%s-%s-%d",muleEvent.getFlowConstruct(),muleContext.getClusterId(),muleEvent.getId(),random.nextInt());
  return new QueueKey(QueuePersistenceObjectStore.DEFAULT_QUEUE_STORE,key);
}
