{
  if (out == null) {
    throw new IllegalStateException("There is no outputstream for this request on: " + context.getEndpointURI() + ". This might be because this is a one way request, but the StreamingBridge component should not be used in this scenario");
  }
  IOUtils.copyLarge(in,out);
  out.close();
}
