{
  File item=new File(store,event.getComponent().getDescriptor().getName() + "/" + event.getId()+ ".e");
  if (item.exists()) {
synchronized (lock) {
      return item.delete();
    }
  }
  return false;
}
