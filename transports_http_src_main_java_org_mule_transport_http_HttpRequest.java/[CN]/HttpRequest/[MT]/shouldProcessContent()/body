{
  String methodName=requestLine.getMethod();
  if (HttpConstants.METHOD_POST.equalsIgnoreCase(methodName) || HttpConstants.METHOD_PUT.equalsIgnoreCase(methodName) || HttpConstants.METHOD_PATCH.equalsIgnoreCase(methodName)) {
    return true;
  }
  return false;
}
