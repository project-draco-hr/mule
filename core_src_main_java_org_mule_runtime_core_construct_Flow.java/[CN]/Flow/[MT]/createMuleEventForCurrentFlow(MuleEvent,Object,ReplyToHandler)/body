{
  event=new DefaultMuleEvent(event,this,event.isAllowNonBlocking() ? replyToHandler : null,replyToDestination,event.isSynchronous() || isSynchronous());
  resetRequestContextEvent(event);
  return event;
}
