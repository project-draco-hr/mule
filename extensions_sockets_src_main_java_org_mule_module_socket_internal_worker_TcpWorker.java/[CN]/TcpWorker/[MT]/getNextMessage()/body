{
  InputStream readMsg=null;
  try {
    readMsg=protocol.read(dataIn);
    if (dataIn.isStreaming()) {
      moreMessages.set(false);
    }
    return readMsg;
  }
  finally {
    if (readMsg == null) {
      dataIn.close();
    }
  }
}
