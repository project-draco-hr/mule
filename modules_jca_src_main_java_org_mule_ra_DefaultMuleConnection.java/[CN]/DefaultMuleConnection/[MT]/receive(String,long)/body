{
  MuleEndpointURI muleEndpoint=new MuleEndpointURI(url);
  UMOEndpoint endpoint=managementContext.getRegistry().getOrCreateEndpointForUri(muleEndpoint,UMOEndpoint.ENDPOINT_TYPE_SENDER);
  try {
    return endpoint.receive(timeout);
  }
 catch (  Exception e) {
    throw new ReceiveException(endpoint,timeout,e);
  }
}
