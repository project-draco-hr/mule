{
  endpoint.getConnector().startConnector();
  Mock mockComponent=new Mock(UMOComponent.class);
  read.deleteOnExit();
  move.deleteOnExit();
  return new FileMessageReceiver(endpoint.getConnector(),(UMOComponent)mockComponent.proxy(),endpoint,read.getAbsolutePath(),move.getAbsolutePath(),null,new Long(1000));
}
