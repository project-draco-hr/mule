{
  readOnly=false;
  initializeAdded();
  List<Object> queueAdded=lookupQueue(queue);
  if (queue.offer(null,queueAdded.size(),offerTimeout)) {
    queueAdded.add(item);
    return true;
  }
 else {
    return false;
  }
}
