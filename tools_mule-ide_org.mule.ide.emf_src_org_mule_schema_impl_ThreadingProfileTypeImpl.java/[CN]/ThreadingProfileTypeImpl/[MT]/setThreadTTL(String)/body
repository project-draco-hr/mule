{
  String oldThreadTTL=threadTTL;
  threadTTL=newThreadTTL;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,MulePackage.THREADING_PROFILE_TYPE__THREAD_TTL,oldThreadTTL,threadTTL));
}
