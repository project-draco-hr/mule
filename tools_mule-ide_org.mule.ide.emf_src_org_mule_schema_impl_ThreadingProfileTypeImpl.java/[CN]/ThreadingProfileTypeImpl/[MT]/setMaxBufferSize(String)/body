{
  String oldMaxBufferSize=maxBufferSize;
  maxBufferSize=newMaxBufferSize;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,SchemaPackage.THREADING_PROFILE_TYPE__MAX_BUFFER_SIZE,oldMaxBufferSize,maxBufferSize));
}
