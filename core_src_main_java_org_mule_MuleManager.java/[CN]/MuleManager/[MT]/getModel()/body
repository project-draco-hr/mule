{
  if (registry == null) {
    createRegistry();
  }
  if (model == null) {
    model=new SedaModel();
    model.setName(DEFAULT_MODEL_NAME);
    try {
      model.register();
    }
 catch (    UMOException e) {
      logger.error("Unable to register model " + model.getName() + " with the registry");
    }
  }
  return model;
}
