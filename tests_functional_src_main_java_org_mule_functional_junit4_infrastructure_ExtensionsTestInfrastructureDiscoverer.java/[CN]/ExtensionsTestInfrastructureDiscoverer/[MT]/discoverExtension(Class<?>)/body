{
  RuntimeExtensionModel model=loadExtensionModel(new AnnotationsBasedDescriber(annotatedClass,new StaticVersionResolver(getProductVersion())));
  extensionManager.registerExtension(model);
  return model;
}
