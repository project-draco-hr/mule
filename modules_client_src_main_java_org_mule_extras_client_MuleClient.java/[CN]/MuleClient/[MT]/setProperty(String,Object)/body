{
  try {
    managementContext.getRegistry().registerObject(key,value,MuleProperties.OBJECT_MULE_APPLICATION_PROPERTIES,managementContext);
  }
 catch (  RegistrationException e) {
    logger.error(e);
  }
}
