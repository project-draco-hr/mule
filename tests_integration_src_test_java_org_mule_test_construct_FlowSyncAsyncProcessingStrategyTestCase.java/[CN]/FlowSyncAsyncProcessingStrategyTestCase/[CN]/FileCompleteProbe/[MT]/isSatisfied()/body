{
  if (testFile.exists()) {
    try {
      output=FileUtils.readFileToString(testFile);
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
    return "Part 2Part 1".equals(output);
  }
 else {
    return false;
  }
}
