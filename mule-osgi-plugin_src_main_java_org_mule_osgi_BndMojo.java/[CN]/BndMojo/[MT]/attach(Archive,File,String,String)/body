{
  String version=a.getVersion() + "-" + versionAppend;
  Artifact artifact=artifactFactory.createArtifactWithClassifier(a.getGroupId(),a.getArtifactId(),version,type,classifier);
  artifact.setFile(copy);
  project.addAttachedArtifact(artifact);
}
