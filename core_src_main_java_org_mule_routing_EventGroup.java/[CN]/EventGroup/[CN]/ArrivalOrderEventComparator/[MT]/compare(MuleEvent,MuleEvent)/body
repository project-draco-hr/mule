{
  int val1=event1.getFlowVariable(MULE_ARRIVAL_ORDER_PROPERTY) != null ? event1.getFlowVariable(MULE_ARRIVAL_ORDER_PROPERTY) : -1;
  int val2=event2.getFlowVariable(MULE_ARRIVAL_ORDER_PROPERTY) != null ? event1.getFlowVariable(MULE_ARRIVAL_ORDER_PROPERTY) : -1;
  return val1 - val2;
}
