{
  MuleSession session=new DefaultMuleSession(((MuleEvent)events.retrieve(events.allKeys().get(0))).getSession());
  for (int i=1; i < events.allKeys().size(); i++) {
    session.getProperties().putAll(((MuleEvent)events.retrieve(events.allKeys().get(i))).getSession().getProperties());
  }
  return session;
}
