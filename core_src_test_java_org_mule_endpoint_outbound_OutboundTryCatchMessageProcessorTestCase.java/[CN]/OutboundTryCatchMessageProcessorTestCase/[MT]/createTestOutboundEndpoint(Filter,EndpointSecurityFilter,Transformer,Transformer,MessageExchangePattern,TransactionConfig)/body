{
  OutboundEndpoint endpoint=super.createTestOutboundEndpoint(filter,securityFilter,transformer,responseTransformer,exchangePattern,txConfig);
  endpoint.getConnector().setExceptionListener(exceptionListener);
  return endpoint;
}
