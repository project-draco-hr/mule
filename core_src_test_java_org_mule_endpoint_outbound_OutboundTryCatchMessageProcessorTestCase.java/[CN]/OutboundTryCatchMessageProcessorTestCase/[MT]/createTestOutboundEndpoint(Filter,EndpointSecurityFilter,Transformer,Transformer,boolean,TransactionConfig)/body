{
  OutboundEndpoint endpoint=super.createTestOutboundEndpoint(filter,securityFilter,transformer,responseTransformer,sync,txConfig);
  endpoint.getConnector().setExceptionListener(exceptionListener);
  return endpoint;
}
