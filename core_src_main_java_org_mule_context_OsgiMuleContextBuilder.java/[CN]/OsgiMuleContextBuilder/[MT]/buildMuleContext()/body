{
  logger.debug("Building new OsgiMuleContext with BundleContext = " + bundleContext);
  MuleContext muleContext=new OsgiMuleContext(getMuleConfiguration(),getWorkManager(),getWorkListener(),getLifecycleManager(),getNotificationManager(),bundleContext);
  return muleContext;
}
