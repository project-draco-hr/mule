{
  if (name == null) {
    name="WorkManager#" + hashCode();
  }
  syncWorkExecutorPool=new NullWorkExecutorPool(poolSize,name);
  startWorkExecutorPool=new NullWorkExecutorPool(poolSize,name);
  scheduledWorkExecutorPool=new NullWorkExecutorPool(poolSize,name);
}
