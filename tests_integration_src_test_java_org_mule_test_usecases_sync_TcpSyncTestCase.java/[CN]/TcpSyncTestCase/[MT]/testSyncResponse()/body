{
  MuleClient client=new MuleClient();
  UMOMessage message=client.send("tcp://localhost:4444","payload",null);
  assertNotNull(message);
  assertEquals("<string>payload</string>",message.getPayloadAsString());
}
