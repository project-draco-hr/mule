{
  Flow flow=getTestFlow();
  final Event event=Event.builder(DefaultEventContext.create(flow,TEST_CONNECTOR)).message(inMessage).flow(flow).session(getTestSession(null,muleContext)).build();
  return populateFieldsFromInboundEndpoint(event,ep);
}
