{
  final Event event=eventBuilder().message(InternalMessage.builder().payload(RESPONSE_MESSAGE).build()).session(new DefaultMuleSession()).build();
  return populateFieldsFromInboundEndpoint(event,ep);
}
