{
  final boolean keepSendSocketOpen=connector.isKeepSendSocketOpen();
  if (connectedSocket == null || connectedSocket.isClosed() || !keepSendSocketOpen)   connectedSocket=initSocket(endpoint.getEndpointURI().getAddress());
}
