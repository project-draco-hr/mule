{
  bridge.initialise();
  bridge.start();
  MuleEvent response=directInboundMessageSource.process(MuleTestUtils.getTestInboundEvent("hello",muleContext));
  assertEquals("hello",response.getMessageAsString());
}
