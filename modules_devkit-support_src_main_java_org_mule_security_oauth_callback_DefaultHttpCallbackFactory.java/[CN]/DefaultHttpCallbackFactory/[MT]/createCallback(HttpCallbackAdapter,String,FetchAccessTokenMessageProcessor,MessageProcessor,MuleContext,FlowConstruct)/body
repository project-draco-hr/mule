{
  return new DefaultHttpCallback(this.buildCallbackProcessorList(new ExtractAuthorizationCodeMessageProcessor(Pattern.compile(authCodeRegex)),fetchAccessTokenMessageProcessor,listener),muleContext,adapter.getDomain(),adapter.getLocalPort(),adapter.getRemotePort(),adapter.getPath(),adapter.getAsync(),flowConstruct.getExceptionListener(),adapter.getConnector());
}
