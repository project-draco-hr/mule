{
  return new DefaultHttpCallback(this.buildCallbackProcessor(authCodeRegex,fetchAccessTokenMessageProcessor,listener),muleContext,adapter.getDomain(),adapter.getLocalPort(),adapter.getRemotePort(),adapter.getPath(),adapter.getAsync(),flowConstruct.getExceptionListener(),adapter.getConnector());
}
