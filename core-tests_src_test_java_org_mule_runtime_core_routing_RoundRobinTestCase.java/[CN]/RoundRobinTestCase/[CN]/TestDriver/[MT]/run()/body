{
  for (int i=0; i < numMessages; i++) {
    MuleMessage msg=MuleMessage.builder().payload(TEST_MESSAGE + messageNumber.getAndIncrement()).build();
    MuleEvent event=new DefaultMuleEvent(DefaultMessageContext.create(flowConstruct,TEST_CONNECTOR),msg,REQUEST_RESPONSE,flowConstruct,session);
    try {
      target.process(event);
    }
 catch (    MuleException e) {
    }
  }
}
