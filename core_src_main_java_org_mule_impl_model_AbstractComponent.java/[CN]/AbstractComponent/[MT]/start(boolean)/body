{
  registerListeners();
  connectListeners();
  if (stopped.get()) {
    stopped.set(false);
    paused.set(false);
    doStart();
  }
  MuleManager.getInstance().fireNotification(getNotificationStarted());
  if (startPaused) {
    pause();
  }
  startListeners();
}
