{
  String result=null;
  if (pattern != null && pattern.indexOf('{') > -1) {
    result=getFilename(event,pattern,'{','}');
  }
 else {
    result=getFilename(event,pattern,'[',']');
  }
  return result;
}
