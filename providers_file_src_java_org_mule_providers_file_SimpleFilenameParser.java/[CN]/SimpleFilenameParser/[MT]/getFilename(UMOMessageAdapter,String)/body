{
  String result=null;
  if (pattern != null && pattern.indexOf('{') > -1) {
    result=getFilename(adaptor,pattern,'{','}');
  }
 else {
    result=getFilename(adaptor,pattern,'[',']');
  }
  return result;
}
