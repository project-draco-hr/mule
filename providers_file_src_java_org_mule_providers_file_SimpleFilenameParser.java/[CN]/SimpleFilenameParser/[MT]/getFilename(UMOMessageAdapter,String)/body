{
  String result=null;
  if (pattern != null && pattern.indexOf('{') > -1) {
    result=getFilename(adapter,pattern,'{','}');
  }
 else {
    result=getFilename(adapter,pattern,'[',']');
  }
  return result;
}
