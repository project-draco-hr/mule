{
  if (path == null) {
    return null;
  }
  if (dirPath == null) {
    return path;
  }
  if (path.startsWith("/") || path.startsWith("\\")) {
    return path;
  }
  return dirPath + File.separator + path;
}
