{
  boolean success=false;
  this.bootstrap=createBootstrap();
  try {
    this.bootstrap.init(this);
    success=true;
  }
  finally {
    if (!success) {
      this.bootstrap.cleanUp();
    }
  }
}
