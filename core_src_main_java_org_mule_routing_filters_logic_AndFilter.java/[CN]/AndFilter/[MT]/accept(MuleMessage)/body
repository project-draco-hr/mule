{
  if (getFilters().size() == 0) {
    return false;
  }
  for (  Object o : getFilters()) {
    Filter filter=(Filter)o;
    if (!filter.accept(message)) {
      return false;
    }
  }
  return true;
}
