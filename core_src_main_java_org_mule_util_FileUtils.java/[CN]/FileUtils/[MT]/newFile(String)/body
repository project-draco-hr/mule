{
  try {
    return new File(pathName).getCanonicalFile();
  }
 catch (  IOException e) {
    throw new MuleRuntimeException(Message.createStaticMessage("Unable to create a canonical file for " + pathName),e);
  }
}
