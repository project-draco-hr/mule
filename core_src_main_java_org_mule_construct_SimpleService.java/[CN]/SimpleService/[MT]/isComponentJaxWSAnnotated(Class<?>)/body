{
  try {
    return componentClass.getAnnotation((Class<Annotation>)Thread.currentThread().getContextClassLoader().loadClass("javax.jws.WebService")) != null;
  }
 catch (  ClassNotFoundException cnfe) {
    return false;
  }
}
