{
  UMOSession session=MuleManager.getInstance().getModel().getComponentSession("component1");
  UMOEvent event=new MuleEvent(new MuleMessage("Test Event"),(UMOEndpoint)session.getComponent().getDescriptor().getInboundRouter().getEndpoints().get(0),session,true);
  session.sendEvent(event);
}
