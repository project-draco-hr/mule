{
  if (logger.isDebugEnabled()) {
    logger.debug(MessageFormat.format("{1}: Putting event back on queue {2}",queue.getName(),getStageDescription(),event));
  }
  try {
    queue.untake(event);
  }
 catch (  Exception e) {
    logger.error(e);
  }
}
