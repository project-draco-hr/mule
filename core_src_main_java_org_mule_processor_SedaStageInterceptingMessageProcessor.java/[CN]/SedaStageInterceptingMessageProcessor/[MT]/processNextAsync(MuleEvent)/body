{
  try {
    if (isStatsEnabled()) {
      queueStatistics.incQueuedEvent();
    }
    enqueue(DefaultMuleEvent.copy(event));
  }
 catch (  Exception e) {
    throw new FailedToQueueEventException(CoreMessages.interruptedQueuingEventFor(getStageDescription()),event,e);
  }
  if (logger.isTraceEnabled()) {
    logger.trace("MuleEvent added to queue for: " + getStageDescription());
  }
}
