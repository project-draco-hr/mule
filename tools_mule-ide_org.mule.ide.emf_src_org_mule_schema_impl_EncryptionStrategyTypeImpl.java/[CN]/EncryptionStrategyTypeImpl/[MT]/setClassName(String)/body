{
  String oldClassName=className;
  className=newClassName;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,SchemaPackage.ENCRYPTION_STRATEGY_TYPE__CLASS_NAME,oldClassName,className));
}
