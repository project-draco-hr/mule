{
  try {
    JmsThreadContext ctx=context.getContext();
    if (ctx.consumer == null) {
      createConsumer();
    }
    super.poll();
  }
 catch (  Exception e) {
    closeConsumer(true);
    throw e;
  }
 finally {
    closeConsumer(false);
  }
}
