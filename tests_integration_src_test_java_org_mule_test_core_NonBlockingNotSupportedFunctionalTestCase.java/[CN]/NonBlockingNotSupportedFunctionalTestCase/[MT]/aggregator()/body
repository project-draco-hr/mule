{
  String correlationId="id";
  int correlationGroupSize=3;
  flowRunner("aggregator").withPayload(TEST_MESSAGE).withSourceCorrelationId(correlationId).withCorrelation(new Correlation(correlationGroupSize,1)).nonBlocking().runNoVerify();
  flowRunner("aggregator").withPayload(TEST_MESSAGE).withSourceCorrelationId(correlationId).withCorrelation(new Correlation(correlationGroupSize,2)).nonBlocking().runNoVerify();
  flowRunner("aggregator").withPayload(TEST_MESSAGE).withSourceCorrelationId(correlationId).withCorrelation(new Correlation(correlationGroupSize,3)).nonBlocking().run();
}
