{
  return Collections.<String,Object>singletonMap(LocatedMuleException.INFO_LOCATION_KEY,resolveProcessorRepresentation(event.getMuleContext().getConfiguration().getId(),getProcessorPath(event,lastProcessed),lastProcessed));
}
