{
  AbstractConnector connector=(AbstractConnector)endpoint.getConnector();
  Service service=event.getService();
  if (connector.isEnableMessageEvents()) {
    connector.fireNotification(new EndpointMessageNotification(event.getMessage(),endpoint,service.getName(),EndpointMessageNotification.MESSAGE_RECEIVED));
  }
}
