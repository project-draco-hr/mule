{
  String uri=protocol + "://" + USER+ ":"+ PASSWORD+ "@"+ LOCALHOST+ ":"+ port+ "?connector="+ connectorName;
  if (!transformInboundMessage()) {
    uri=uri + "&transformers=noActionTransformer";
  }
  return uri;
}
