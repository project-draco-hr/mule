{
  MuleClient client=muleContext.getClient();
  final JSR250LifecycleTrackerComponent ltc=(JSR250LifecycleTrackerComponent)client.send("vm://" + serviceName + ".In",new DefaultMuleMessage(NullPayload.getInstance(),muleContext)).getPayload();
  assertNotNull(ltc);
  return ltc;
}
