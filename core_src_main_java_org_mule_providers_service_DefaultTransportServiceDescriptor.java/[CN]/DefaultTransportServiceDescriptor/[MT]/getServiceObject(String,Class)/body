{
  Object service;
  try {
    if (classType == null) {
      service=context.getBean(name);
    }
 else {
      service=context.getBean(name,classType);
    }
    return service;
  }
 catch (  BeansException e) {
    throw new TransportServiceException(new Message(Messages.FAILED_TO_CREATE_X_WITH_X,name,classType),e);
  }
}
