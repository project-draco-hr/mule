{
  if (user != null) {
    message.setOutboundProperty(MULE_USER_PROPERTY,MuleCredentials.createHeader(user.getUsername(),user.getPassword()));
  }
  return new DefaultMuleEvent(message,exchangePattern,new MuleClientFlowConstruct(muleContext));
}
