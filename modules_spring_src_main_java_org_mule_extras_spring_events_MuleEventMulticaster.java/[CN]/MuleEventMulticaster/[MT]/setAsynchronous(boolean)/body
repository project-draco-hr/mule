{
  this.asynchronous=asynchronous;
  if (asynchronous) {
    if (asyncPool == null) {
      asyncPool=RegistryContext.getConfiguration().getDefaultThreadingProfile().createPool("spring-events");
    }
  }
 else {
    if (asyncPool != null) {
      asyncPool.shutdown();
      asyncPool=null;
    }
  }
}
