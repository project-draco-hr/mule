{
  for (int i=0; i < subscriptions.length; i++) {
    String subscription=MapUtils.getString(MuleManager.getRegistry().getEndpointIdentifiers(),subscriptions[i],subscriptions[i]);
    ObjectFilter filter=createFilter(subscription);
    if (filter.accept(endpoint)) {
      return true;
    }
  }
  return false;
}
