{
  if (!(cookieSpec.equalsIgnoreCase(COOKIE_SPEC_NETSCAPE) || cookieSpec.equalsIgnoreCase(COOKIE_SPEC_RFC2109))) {
    throw new IllegalArgumentException(CoreMessages.propertyHasInvalidValue("cookieSpec",cookieSpec).toString());
  }
  this.cookieSpec=cookieSpec;
}
