{
  Transaction tx=TransactionCoordination.getInstance().getTransaction();
  if (tx != null) {
    try {
      tx.commit();
    }
 catch (    TransactionException e) {
      logger.error(e);
    }
  }
}
