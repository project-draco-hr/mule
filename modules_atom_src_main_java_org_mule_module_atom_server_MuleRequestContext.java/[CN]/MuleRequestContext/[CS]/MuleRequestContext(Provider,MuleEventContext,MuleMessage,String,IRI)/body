{
  super(context,getMethod(request),new IRI(contextPath),baseIri);
  this.baseIri=baseIri;
  this.request=request;
  this.event=event;
  this.session=null;
  principal=null;
  subject=context.resolveSubject(this);
  target=context.resolveTarget(this);
}
