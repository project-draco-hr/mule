{
  assertAccess(READ);
  if (cache != null) {
    return cache;
  }
  byte[] result=getPayload(byte[].class);
  if (muleContext.getConfiguration().isCacheMessageAsBytes()) {
    cache=result;
  }
  return result;
}
