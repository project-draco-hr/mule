{
  Destination dest=super.createDestination(session,name,topic);
  if (dest != null)   return dest;
 else   throw new JMSException("Oracle JMS was unable to bind to the " + (topic ? "topic" : "queue") + ": "+ name+ " but gives no exception nor error message to explain why (that's what you get for using proprietary software...)");
}
