{
  QueueSession queueSession=getTestQueueSession();
  logger.warn("SEDA Queue: " + queueSession.getQueue("out").size() + ", Outbound endpoint vm queue: "+ queueSession.getQueue(service.getName() + ".service").size());
  assertEquals(numMessages,queueSession.getQueue("out").size() + queueSession.getQueue(service.getName() + ".service").size());
}
