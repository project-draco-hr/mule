{
  MuleMessage response=muleContext.getClient().send(String.format("http://localhost:%d/services/%s",dynamicPort.getNumber(),path),getTestMuleMessage(getRequestPayload(soapMethod)),HTTP_REQUEST_OPTIONS);
  assertFault(faultTemplate,response.getPayloadAsString(),faultMessage);
}
