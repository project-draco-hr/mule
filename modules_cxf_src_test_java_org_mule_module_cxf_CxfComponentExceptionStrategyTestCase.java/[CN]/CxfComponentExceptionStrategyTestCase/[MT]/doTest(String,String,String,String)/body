{
  MuleMessage response=muleContext.getClient().send(String.format("http://localhost:%d/services/%s",dynamicPort.getNumber(),path),new DefaultMuleMessage(getRequestPayload(soapMethod),muleContext),HTTP_REQUEST_OPTIONS);
  assertFault(faultTemplate,response.getPayloadAsString(),faultMessage);
}
