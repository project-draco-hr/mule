{
  MuleClient client=muleContext.getClient();
  try {
    client.send("cxf:" + ((InboundEndpoint)muleContext.getRegistry().lookupObject("cxfExceptionStrategyInbound")).getAddress() + "?method=testNonCxfException","TEST",null);
    fail("Exception expected");
  }
 catch (  DispatchException e) {
    assertTrue(e.getCause() instanceof Fault);
  }
}
