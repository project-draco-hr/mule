{
  return new Thread(){
    @Override public void run(){
      for (int i=0; i < times; i++) {
        try {
          callback.execute(data);
        }
 catch (        Exception e) {
          throw new RuntimeException(e);
        }
      }
    }
  }
;
}
