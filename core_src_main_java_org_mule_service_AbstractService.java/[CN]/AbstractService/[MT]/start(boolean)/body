{
  component.start();
  registerListeners();
  connectListeners();
  if (stopped.get()) {
    stopped.set(false);
    paused.set(false);
    doStart();
  }
  fireServiceNotification(ServiceNotification.SERVICE_STARTED);
  if (startPaused) {
    pause();
  }
  startListeners();
}
