{
  component.start();
  registerListeners();
  connectListeners();
  if (stopped.get()) {
    stopped.set(false);
    paused.set(false);
    doStart();
  }
  fireComponentNotification(ServiceNotification.SERVICE_STARTED);
  if (startPaused) {
    pause();
  }
  return startListeners();
}
