{
  if (event.isSynchronous()) {
    MuleMessage result=sendEvent(event);
    if (result != null) {
      return new DefaultMuleEvent(result,event);
    }
 else {
      return null;
    }
  }
 else {
    dispatchEvent(event);
    return null;
  }
}
