{
  String url;
  StringBuilder buf=new StringBuilder(128);
  buf.append(urlPrefix);
  buf.append(host);
  if (port != null && port > 0) {
    buf.append(":");
    buf.append(port);
  }
  buf.append("/");
  if (database != null) {
    buf.append(database);
  }
  url=buf.toString();
  return url;
}
