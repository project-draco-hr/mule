{
  if (muleContext.getConfiguration().isDisableTimeouts()) {
    return WAIT_FOR_EVER;
  }
 else {
    return responseTimeout.getRawValue() != null ? responseTimeout.resolveIntegerValue(muleEvent) : muleContext.getConfiguration().getDefaultResponseTimeout();
  }
}
