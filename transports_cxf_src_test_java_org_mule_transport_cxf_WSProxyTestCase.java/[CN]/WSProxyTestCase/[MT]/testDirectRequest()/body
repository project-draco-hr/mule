{
  MuleClient client=new MuleClient();
  MuleMessage result=client.send("wsdl-cxf:http://localhost:6065/WebService?wsdl&method=echo",new DefaultMuleMessage("mule"));
  assertEquals("mule",result.getPayloadAsString());
}
