{
  MuleClient client=muleContext.getClient();
  InternalMessage response=client.send("http://localhost:" + dynamicPort.getNumber() + "/redirect/products?retrieve=all&order=desc",getTestMuleMessage(TEST_MESSAGE)).getRight();
  assertEquals("Successfully redirected: products?retrieve=all&order=desc",getPayloadAsString(response));
}
