{
  HttpCallback callback=new DefaultHttpCallback(Arrays.asList(processor),muleContext,"localhost",localPort.getNumber(),remotePort.getNumber(),CALLBACK_PATH,false,connector);
  callback.start();
  return callback;
}
