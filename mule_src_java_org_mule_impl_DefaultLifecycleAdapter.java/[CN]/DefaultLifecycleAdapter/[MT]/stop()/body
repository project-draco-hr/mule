{
  if (isStoppable) {
    try {
      ((Stoppable)component).stop();
    }
 catch (    Exception e) {
      throw new MuleException("Failed to stop Mule UMO: " + descriptor.getName(),e);
    }
  }
  started=false;
}
