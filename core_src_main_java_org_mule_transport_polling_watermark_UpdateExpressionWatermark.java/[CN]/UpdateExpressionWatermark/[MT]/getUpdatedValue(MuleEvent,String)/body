{
  try {
    return StringUtils.isEmpty(this.updateExpression) ? event.getFlowVariable(variableName) : WatermarkUtils.evaluate(this.updateExpression,event);
  }
 catch (  NotSerializableException e) {
    throw new IllegalArgumentException(e);
  }
}
