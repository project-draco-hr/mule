{
  ((DefaultMuleEvent)event).setLegacyCorrelationId(getCid());
  ((DefaultMuleEvent)event).setCorrelation(new Correlation(event.getCorrelation().getGroupSize().orElse(null),event.getCorrelation().getSequence().orElse(null)));
  return event.getMessage();
}
