{
  ((DefaultMuleEvent)event).setCorrelation(new Correlation(getCid(),event.getCorrelation().getGroupSize().orElse(null),event.getCorrelation().getSequence().orElse(null)));
  return event.getMessage();
}
