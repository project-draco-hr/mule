{
  this.model=model;
  if (model instanceof MuleModel) {
    ((MuleModel)model).setListeners(eventManager);
  }
  if (initialised.get()) {
    model.initialise();
  }
  if (started.get()) {
    model.start();
  }
}
