{
  if (realm == null) {
    if (isRealmRequired()) {
      throw new InitialisationException(SpringSecurityMessages.authRealmMustBeSetOnFilter(),this);
    }
 else {
      logger.warn("There is no security realm set, using default: null");
    }
  }
}
