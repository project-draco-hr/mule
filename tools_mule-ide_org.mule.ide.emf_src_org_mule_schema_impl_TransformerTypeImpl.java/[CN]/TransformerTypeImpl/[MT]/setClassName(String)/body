{
  String oldClassName=className;
  className=newClassName;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,SchemaPackage.TRANSFORMER_TYPE__CLASS_NAME,oldClassName,className));
}
