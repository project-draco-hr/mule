{
  MuleMessage mockMessage=Mockito.mock(MuleMessage.class);
  Banana b=new Banana();
  Mockito.when(mockMessage.getPayload()).thenReturn("a");
  assertEquals("ab",evaluate("message.transformedPayload(new org.mule.transformer.simple.StringAppendTransformer('b'))",mockMessage));
}
