{
  if (!(builder instanceof AbstractEndpointBuilder)) {
    return MessageExchangePattern.ONE_WAY;
  }
  return ((AbstractEndpointBuilder)builder).messageExchangePattern != null ? ((AbstractEndpointBuilder)builder).messageExchangePattern : MessageExchangePattern.ONE_WAY;
}
