{
  LOGGER.debug("Intercepting operation '{}' completed",operationContext.getOperationModel().getName());
  try {
    interceptingCallback.onComplete();
  }
 catch (  Exception e) {
    throw new MessagingException(createStaticMessage(format("Intercepting operation '%s' failed to notify completion",operationContext.getOperationModel().getName())),event,e,this);
  }
}
