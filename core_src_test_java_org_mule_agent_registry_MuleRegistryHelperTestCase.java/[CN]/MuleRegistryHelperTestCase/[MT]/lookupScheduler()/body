{
  Scheduler scheduler=scheduler();
  register(scheduler);
  assertEquals(scheduler,muleContext.getRegistry().lookupScheduler(new Predicate<String>(){
    @Override public boolean evaluate(    String s){
      return s.equalsIgnoreCase("schedulerName");
    }
  }
).iterator().next());
}
