{
  Flow flow=mock(Flow.class);
  when(flow.getProcessingStrategy()).thenReturn(new NonBlockingProcessingStrategy());
  when(flow.getMuleContext()).thenReturn(muleContext);
  return new DefaultMuleEvent(DefaultMessageContext.create(flow,TEST_CONNECTOR),MuleMessage.builder().payload(TEST_MESSAGE).build(),MessageExchangePattern.REQUEST_RESPONSE,nullReplyToHandler,flow);
}
