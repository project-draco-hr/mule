{
  if (addr == null)   return null;
  String ret="";
  for (int i=0; i < addr.length; i++) {
    ret+=addr[i].toString() + ",";
  }
  if (ret.length() > 0)   ret=ret.substring(0,ret.length() - 1);
  return ret;
}
