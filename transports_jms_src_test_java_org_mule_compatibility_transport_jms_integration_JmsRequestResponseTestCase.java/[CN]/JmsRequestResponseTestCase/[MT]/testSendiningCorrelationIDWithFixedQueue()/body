{
  InternalMessage response=flowRunner("JMSCorrelationIDFixedQueue").nonBlocking().withPayload(TEST_MESSAGE).run().getMessage();
  verify("JMSCorrelationIDFixedQueue");
  verify("JMSCorrelationIDTarget");
  assertFixedEchoResponse(response);
}
