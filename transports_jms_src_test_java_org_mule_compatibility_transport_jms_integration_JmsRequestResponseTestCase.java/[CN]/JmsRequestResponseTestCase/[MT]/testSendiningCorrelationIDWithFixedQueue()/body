{
  InternalMessage response=flowRunner("JMSCorrelationIDFixedQueue").withPayload(TEST_MESSAGE).run().getMessage();
  verify("JMSCorrelationIDFixedQueue");
  verify("JMSCorrelationIDTarget");
  assertFixedEchoResponse(response);
}
