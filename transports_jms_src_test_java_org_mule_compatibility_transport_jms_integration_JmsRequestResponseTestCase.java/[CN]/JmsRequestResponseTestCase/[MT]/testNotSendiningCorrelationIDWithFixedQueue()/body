{
  InternalMessage response=flowRunner("JMSNoCorrelationIDFixedQueue").nonBlocking().withPayload(TEST_MESSAGE).run().getMessage();
  verify("JMSNoCorrelationIDFixedQueue");
  verify("JMSNoCorrelationIDTarget");
  assertEchoResponse(response);
}
