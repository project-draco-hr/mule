{
  try {
    RequestContext.setEvent(event);
    doDispatch(event);
  }
 catch (  Exception e) {
    dispose();
    getConnector().handleException(e);
  }
 finally {
    if (disposeOnCompletion) {
      dispose();
    }
  }
}
