{
  ComponentConfiguration componentConfiguration=new ComponentConfiguration.Builder().setNamespace("mule").setIdentifier("configuration").build();
  expectedException.expect(BundleNotFoundException.class);
  getConnectionTestingService(componentConfiguration);
}
