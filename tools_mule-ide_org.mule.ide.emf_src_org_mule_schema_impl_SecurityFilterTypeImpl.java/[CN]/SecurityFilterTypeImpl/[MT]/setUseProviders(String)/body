{
  String oldUseProviders=useProviders;
  useProviders=newUseProviders;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,MulePackage.SECURITY_FILTER_TYPE__USE_PROVIDERS,oldUseProviders,useProviders));
}
