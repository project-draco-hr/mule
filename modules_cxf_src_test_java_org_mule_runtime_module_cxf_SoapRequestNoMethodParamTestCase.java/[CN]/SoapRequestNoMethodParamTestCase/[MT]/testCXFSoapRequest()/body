{
  InternalMessage msg=muleContext.getClient().send("http://localhost:" + port1.getValue() + "/services/TestComponent",InternalMessage.of(request),HTTP_REQUEST_OPTIONS).getRight();
  assertNotNull(msg);
  assertNotNull(msg.getPayload().getValue());
  assertEquals(response,getPayloadAsString(msg));
}
