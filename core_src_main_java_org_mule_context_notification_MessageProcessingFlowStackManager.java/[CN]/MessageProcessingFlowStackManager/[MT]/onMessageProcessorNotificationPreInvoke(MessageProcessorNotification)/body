{
  if (DefaultMuleConfiguration.flowCallStacks) {
    notification.getSource().getFlowCallStack().peek().addInvokedMessageProcessor(resolveProcessorRepresentation(muleContext.getConfiguration().getId(),notification.getProcessorPath(),notification.getProcessor()));
  }
}
