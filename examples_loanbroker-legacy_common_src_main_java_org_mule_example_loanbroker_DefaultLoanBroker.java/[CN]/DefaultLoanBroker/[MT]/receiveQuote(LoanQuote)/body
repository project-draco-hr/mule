{
  int quoteNumber=incQuotes();
  if (logger.isInfoEnabled()) {
    String[] params=new String[]{String.valueOf(quoteNumber),quote.toString()};
    logger.info("\n***** " + LocaleMessage.receivedQuote(params));
  }
  return quote;
}
