{
  int profileNumber=incProfiles();
  if (logger.isInfoEnabled()) {
    String[] params=new String[]{String.valueOf(profileNumber),String.valueOf(profile.getCreditScore()),String.valueOf(profile.getCreditHistory())};
    logger.info("\n***** " + LocaleMessage.receivedProfile(params));
  }
  return profile;
}
