{
  try {
    return message.getPayloadAsString();
  }
 catch (  Exception e) {
    throw new MuleException("Failed while retrieving the current payload. Message is of type " + message.getPayload().getClass().getName(),e);
  }
}
