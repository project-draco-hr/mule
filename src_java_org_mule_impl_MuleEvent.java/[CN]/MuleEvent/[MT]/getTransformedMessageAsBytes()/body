{
  try {
    return Utility.objectToByteArray(getTransformedMessage());
  }
 catch (  IOException e) {
    throw new TransformerException(new Message(Messages.CANT_READ_PAYLOAD_AS_BYTES_TYPE_IS_X,getTransformedMessage().getClass().getName()),endpoint.getTransformer(),e);
  }
}
