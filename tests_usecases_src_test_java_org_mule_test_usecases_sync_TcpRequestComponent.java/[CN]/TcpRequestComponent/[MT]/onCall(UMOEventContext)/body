{
  UMOMessage msg=new MuleMessage(eventContext.getMessageAsString(),null);
  msg.setCorrelationGroupSize(1);
  msg.setCorrelationId(msg.getUniqueId());
  eventContext.dispatchEvent(msg);
  return msg;
}
