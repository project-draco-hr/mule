{
  String oldFactory=factory;
  factory=newFactory;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,MulePackage.FACTORY_PROPERTY_TYPE__FACTORY,oldFactory,factory));
}
