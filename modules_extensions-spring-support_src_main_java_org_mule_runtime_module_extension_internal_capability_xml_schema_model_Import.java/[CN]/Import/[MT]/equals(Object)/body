{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  Import anImport=(Import)o;
  return StringUtils.equals(getNamespace(),anImport.getNamespace()) && StringUtils.equals(getSchemaLocation(),anImport.getSchemaLocation());
}
