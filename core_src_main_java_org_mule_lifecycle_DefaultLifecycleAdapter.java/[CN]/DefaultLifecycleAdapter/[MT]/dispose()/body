{
  if (isDisposable) {
    try {
      ((Disposable)componentObject).dispose();
    }
 catch (    Exception e) {
      logger.error("failed to dispose: " + service.getName(),e);
    }
  }
  disposed=true;
}
