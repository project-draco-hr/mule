{
  if (isStoppable) {
    try {
      ((Stoppable)component).stop();
    }
 catch (    Exception e) {
      throw new DefaultMuleException(CoreMessages.failedToStop("UMO Component: " + component.getName()),e);
    }
  }
  started=false;
}
