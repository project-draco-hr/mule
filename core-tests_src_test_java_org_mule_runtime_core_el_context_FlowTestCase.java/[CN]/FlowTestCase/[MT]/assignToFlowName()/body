{
  MuleEvent event=MuleEvent.builder(context).message(MuleMessage.builder().payload("").build()).exchangePattern(ONE_WAY).flow(flowConstruct).build();
  assertFinalProperty("flow.name='foo'",event);
}
