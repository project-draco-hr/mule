{
  InternalMessage response=muleContext.getClient().send("http://localhost:" + httpPortProxy.getNumber() + "/body",getTestMuleMessage(getAllRequest),HTTP_REQUEST_OPTIONS).getRight();
  assertTrue(XMLUnit.compareXML(getAllResponse,getPayloadAsString(response)).identical());
}
