{
  String expression=((OGNLFilter)((FilteringOutboundRouter)muleContext.getRegistry().lookupService(SERVICE_NAME).getOutboundRouter().getRoutes().get(0)).getFilter()).getExpression();
  assertEquals(expression,OGNL_EXSPRESSION);
}
