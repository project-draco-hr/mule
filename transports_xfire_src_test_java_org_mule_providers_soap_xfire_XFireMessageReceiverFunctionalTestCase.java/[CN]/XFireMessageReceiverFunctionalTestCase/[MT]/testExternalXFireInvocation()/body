{
  Service serviceModel=new ObjectServiceFactory().create(EchoService.class);
  EchoService echoService=(EchoService)new XFireProxyFactory().create(serviceModel,"http://localhost:63081/services/echoServiceWsdlPortType");
  String response=echoService.echo("hello world");
  assertEquals("hello world",response);
}
