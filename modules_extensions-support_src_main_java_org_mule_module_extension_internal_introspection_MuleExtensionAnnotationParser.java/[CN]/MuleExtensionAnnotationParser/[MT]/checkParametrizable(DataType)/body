{
  if (disallowedOperationParameterTypes.contains(type.getRawType())) {
    throw new IllegalArgumentException(String.format("Type %s is not allowed as an operation parameter. Use dependency injection instead",type.getRawType().getName()));
  }
}
