{
  if (content == null) {
    try {
      content=doGetContent();
    }
 catch (    Exception e) {
      throw new MuleRuntimeException(createStaticMessage("Could not open file " + path),e);
    }
  }
  return content;
}
