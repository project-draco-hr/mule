{
  if (safe && event instanceof ThreadSafeAccess) {
    if (!required) {
      noteUse("event");
    }
    return (UMOEvent)((ThreadSafeAccess)event).newThreadCopy();
  }
 else {
    return event;
  }
}
