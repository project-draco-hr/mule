{
  if (safe && message instanceof ThreadSafeAccess) {
    if (!required) {
      noteUse("message");
    }
    return (UMOMessage)((ThreadSafeAccess)message).newThreadCopy();
  }
 else {
    return message;
  }
}
