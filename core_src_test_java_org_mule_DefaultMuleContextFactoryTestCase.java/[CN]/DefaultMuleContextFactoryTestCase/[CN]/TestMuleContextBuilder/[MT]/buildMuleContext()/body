{
  MuleContextLifecycleManager lifecycleManager=getLifecycleManager();
  MuleContext muleContext=new TestMuleContext(getMuleConfiguration(),getWorkManager(),getWorkListener(),lifecycleManager,getNotificationManager());
  lifecycleManager.setMuleContext(muleContext);
  return muleContext;
}
