{
  for (  Deployer deployer : deployers) {
    try {
      deployer.install();
      deployer.init();
      deployer.start();
    }
 catch (    DeploymentException e) {
      e.printStackTrace();
    }
  }
}
