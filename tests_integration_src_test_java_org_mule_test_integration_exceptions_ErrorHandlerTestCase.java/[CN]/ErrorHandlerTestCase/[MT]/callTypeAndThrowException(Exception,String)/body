{
  MuleMessage response=flowRunner("matchesHandlerUsingType").withPayload("0").withFlowVariable("exception",exception).run().getMessage();
  assertThat(getPayloadAsString(response),is(expectedMessage));
}
