{
  try {
    if (sftpClient != null) {
      sftpClient.disconnect();
    }
    if (sftpServer != null) {
      sftpServer.stop();
    }
  }
  finally {
    temporaryFolder.delete();
    System.clearProperty(BASE_DIR_SYSTEM_PROPERTY);
  }
}
