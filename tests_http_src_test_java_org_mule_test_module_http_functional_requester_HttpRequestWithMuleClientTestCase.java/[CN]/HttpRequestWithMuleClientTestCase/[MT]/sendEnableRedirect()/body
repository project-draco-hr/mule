{
  final MuleMessage response=muleContext.getClient().send(getRedirectUrl(),MuleMessage.builder().nullPayload().build(),newOptions().enableFollowsRedirect().build()).getRight();
  assertThat(getPayloadAsString(response),is(Objects.toString(null)));
}
