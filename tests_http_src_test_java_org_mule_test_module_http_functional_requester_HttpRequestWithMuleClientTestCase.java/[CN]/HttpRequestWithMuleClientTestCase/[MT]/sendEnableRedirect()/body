{
  final InternalMessage response=muleContext.getClient().send(getRedirectUrl(),InternalMessage.builder().nullPayload().build(),newOptions().enableFollowsRedirect().build()).getRight();
  assertThat(getPayloadAsString(response),is(Objects.toString(null)));
}
