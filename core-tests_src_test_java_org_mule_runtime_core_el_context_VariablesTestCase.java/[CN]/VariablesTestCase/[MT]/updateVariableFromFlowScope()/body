{
  MuleMessage message=MuleMessage.builder().payload("").build();
  MuleEvent event=MuleEvent.builder(context).message(message).exchangePattern(ONE_WAY).flow(flowConstruct).addFlowVariable("foo","bar").build();
  assertEquals("bar_new",evaluate("foo='bar_new'",event));
}
