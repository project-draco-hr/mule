{
  MuleClient client=new MuleClient(muleContext);
  MuleMessage result=client.request(((InboundEndpoint)client.getMuleContext().getRegistry().lookupObject("httpInboundAegis")).getAddress() + "?wsdl",5000);
  assertNotNull(result.getPayload());
}
