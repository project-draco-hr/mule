{
  MuleClient client=new MuleClient(muleContext);
  MuleMessage result=client.request(((InboundEndpoint)client.getMuleContext().getRegistry().lookupObject("httpInboundSource")).getAddress() + "?wsdl",5000);
  assertNotNull(result.getPayload());
}
