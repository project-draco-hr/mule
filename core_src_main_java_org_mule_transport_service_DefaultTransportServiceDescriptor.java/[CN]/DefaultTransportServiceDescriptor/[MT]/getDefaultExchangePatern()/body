{
  if (defaultExchangePattern == null) {
    throw new TransportServiceException(CoreMessages.objectNotSetInService("Default exchange pattern",getService()));
  }
  return MessageExchangePattern.fromString(defaultExchangePattern);
}
