{
  MuleClient client=muleContext.getClient();
  MuleMessage result=client.send("errorPropagationEndpoint",MuleMessage.builder().payload(TEST_MESSAGE).mediaType(MediaType.parse("text/plain;charset=UTF-8")).build()).getRight();
  assertThat((String)result.getInboundProperty("http.status"),is("400"));
}
