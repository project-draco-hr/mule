{
  runTestFailingWithExpectedFlowConstructInvalidException(new Callable<WSProxy>(){
    @Override public WSProxy call() throws Exception {
      return new WSProxy("testOneWayOutboundEndpoint",muleContext,getTestInboundEndpoint(MessageExchangePattern.REQUEST_RESPONSE),MuleTestUtils.getTestOutboundEndpoint(MessageExchangePattern.ONE_WAY,muleContext),noMessageProcessors,noMessageProcessors);
    }
  }
);
}
