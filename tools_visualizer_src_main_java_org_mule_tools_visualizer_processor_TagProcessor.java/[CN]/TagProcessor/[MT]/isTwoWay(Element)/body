{
  if (e == null) {
    return getEnvironment().isDefaultTwoWay();
  }
  return ("true".equalsIgnoreCase(e.getAttributeValue(MuleTag.ATTRIBUTE_SYNCHRONOUS)) || getEnvironment().isDefaultTwoWay());
}
