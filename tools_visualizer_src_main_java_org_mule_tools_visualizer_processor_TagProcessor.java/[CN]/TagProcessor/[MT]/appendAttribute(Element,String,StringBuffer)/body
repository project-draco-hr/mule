{
  if (e.getAttribute(name) == null) {
    return;
  }
  String value=e.getAttributeValue(name);
  if (value != null) {
    caption.append(name + " = " + (StringUtils.EMPTY.equals(value) ? "\"\"" : lookupPropertyTemplate(value))+ "\n");
  }
}
