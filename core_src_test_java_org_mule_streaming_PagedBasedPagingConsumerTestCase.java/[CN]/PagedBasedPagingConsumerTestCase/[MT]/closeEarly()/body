{
  final int top=2;
  List<List<Integer>> elements=new ArrayList<List<Integer>>();
  for (int i=0; i < top; i++) {
    elements.add(this.consumer.consume());
  }
  this.consumer.close();
  Assert.assertEquals(top,elements.size());
  Assert.assertTrue(this.consumer.isConsumed());
  this.consumer.consume();
}
