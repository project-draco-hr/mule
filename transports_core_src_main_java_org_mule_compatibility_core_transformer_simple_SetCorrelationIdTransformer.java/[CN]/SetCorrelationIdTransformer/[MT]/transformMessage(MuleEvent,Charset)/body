{
  ((DefaultMuleEvent)event).setCorrelation(new Correlation(correlationIdEvaluator.resolveValue(event).toString(),event.getCorrelation().getGroupSize().orElse(null),event.getCorrelation().getSequence().orElse(null)));
  return event.getMessage();
}
