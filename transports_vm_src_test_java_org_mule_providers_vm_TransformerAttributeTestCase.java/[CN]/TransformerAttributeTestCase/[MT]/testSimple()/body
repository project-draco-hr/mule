{
  UMOMessage message=new MuleClient().send("vm://simple",OUTBOUND_MESSAGE,null);
  assertNotNull(message);
  assertEquals(FunctionalTestComponent.received(StringAppendTestTranformer.appendDefault(OUTBOUND_MESSAGE)),message.getPayloadAsString());
}
