{
  System.out.println("doDispatch " + Thread.currentThread().getName() + " "+ System.currentTimeMillis());
  super.doDispatch(event);
  Thread.sleep(DELAY_TIME);
  counter.incrementAndGet();
  latch.countDown();
}
