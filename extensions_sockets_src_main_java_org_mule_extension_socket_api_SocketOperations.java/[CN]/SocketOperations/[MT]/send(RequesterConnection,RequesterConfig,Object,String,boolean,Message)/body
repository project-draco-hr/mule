{
  SocketClient client=connection.getClient();
  if (outputEncoding == null) {
    outputEncoding=config.getDefaultEncoding();
  }
  client.write(content,outputEncoding);
  return hasResponse ? Result.builder().output(client.read()).attributes(client.getAttributes()).build() : Result.builder(muleMessage).build();
}
