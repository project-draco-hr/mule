{
  this.next=next;
  try {
    this.refreshTokenManager=context.getRegistry().lookupObject(RefreshTokenManager.class);
  }
 catch (  RegistrationException e) {
    throw new IllegalStateException("Could not create interceptor because RefreshTokenManager could not be found in the registry",e);
  }
}
