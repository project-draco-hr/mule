{
  if (messageExchangePattern != null) {
    return messageExchangePattern.hasResponse();
  }
  return synchronous != null ? synchronous.booleanValue() : getDefaultSynchronous(connector,endpointURI.getScheme());
}
