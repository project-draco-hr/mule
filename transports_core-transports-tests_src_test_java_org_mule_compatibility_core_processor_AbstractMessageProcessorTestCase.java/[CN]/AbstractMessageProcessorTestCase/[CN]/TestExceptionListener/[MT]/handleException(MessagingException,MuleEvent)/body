{
  sensedException=exception;
  event.setError(createErrorMock(exception));
  event.setMessage(MuleMessage.builder(event.getMessage()).nullPayload().exceptionPayload(new DefaultExceptionPayload(exception)).build());
  return event;
}
