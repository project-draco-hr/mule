{
  sensedException=exception;
  event.setMessage(MuleMessage.builder(event.getMessage()).payload(NullPayload.getInstance()).exceptionPayload(new DefaultExceptionPayload(exception)).build());
  return event;
}
