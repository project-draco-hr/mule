{
  Map<String,Serializable> props=new HashMap<>();
  props.put("prop1","value1");
  final DefaultMuleEvent event=new DefaultMuleEvent(new DefaultMuleMessage(TEST_MESSAGE,props),getTestFlow(),getTestSession(null,muleContext));
  DefaultMuleEventEndpointUtils.populateFieldsFromInboundEndpoint(event,endpoint);
  return event;
}
