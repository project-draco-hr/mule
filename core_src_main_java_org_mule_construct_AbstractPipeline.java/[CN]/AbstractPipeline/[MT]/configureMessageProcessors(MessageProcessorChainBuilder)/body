{
  getProcessingStrategy().configureProcessors(getMessageProcessors(),new ProcessingStrategy.ThreadNameSource(){
    @Override public String getName(){
      return ThreadNameHelper.flow(muleContext,AbstractPipeline.this.getName(),0);
    }
  }
,builder,muleContext);
}
