{
  Connector connector=endpoint.getConnector();
  connector.start();
  Service mockComponent=mock(Service.class);
  read.deleteOnExit();
  move.deleteOnExit();
  return new FileMessageReceiver(connector,mockComponent,endpoint,read.getAbsolutePath(),move.getAbsolutePath(),null,1000);
}
