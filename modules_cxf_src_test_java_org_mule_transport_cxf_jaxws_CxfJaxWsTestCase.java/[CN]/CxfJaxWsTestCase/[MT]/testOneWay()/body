{
  MuleClient client=new MuleClient(muleContext);
  MuleMessage result=client.send("cxf:http://localhost:63081/services/async?method=send","Hello!",null);
  assertEquals(NullPayload.getInstance(),result.getPayload());
}
