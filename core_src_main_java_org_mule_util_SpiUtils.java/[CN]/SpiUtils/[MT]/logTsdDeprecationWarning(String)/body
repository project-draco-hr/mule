{
  if (logger.isWarnEnabled()) {
    logger.warn(MessageFormat.format("[{0}] transport service descriptor must be moved under {1}{2} " + "Old-style {1}{3} has been deprecated and may not be supported in the future.",transport,SERVICE_ROOT,TRANSPORT_SERVICE_PATH,PROVIDER_SERVICE_PATH));
  }
}
