{
  final String wsdl=muleClient.request("http://localhost:" + getPorts().get(0) + "/weather-forecast/"+ proxyId+ "?wsdl",getTestTimeoutSecs() * 1000L).getPayloadAsString();
  assertTrue(wsdl.contains("GetWeatherByZipCode"));
}
