{
  final String wsdl=muleClient.request("http://localhost:" + port1.getNumber() + "/weather-forecast/"+ proxyId+ "?wsdl",getTestTimeoutMillis()).getPayloadAsString();
  assertTrue(wsdl.contains("GetWeatherByZipCode"));
}
