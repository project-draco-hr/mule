{
  String oldMaxActive=maxActive;
  maxActive=newMaxActive;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,MulePackage.POOLING_PROFILE_TYPE__MAX_ACTIVE,oldMaxActive,maxActive));
}
