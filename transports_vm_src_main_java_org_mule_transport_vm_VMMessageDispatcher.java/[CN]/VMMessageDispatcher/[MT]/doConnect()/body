{
  if (!endpoint.getExchangePattern().hasResponse()) {
    connector.getQueueProfile().configureQueue(connector.getMuleContext(),endpoint.getEndpointURI().getAddress(),connector.getQueueManager());
  }
}
