{
  Map<String,Serializable> props=new HashMap<>();
  props.put("object",new Apple());
  props.put("number",1);
  props.put("string","hello");
  testObject=MuleMessage.builder().payload("test").outboundProperties(props).build();
  Flow flow=getTestFlow();
  setCurrentEvent(new DefaultMuleEvent(DefaultMessageContext.create(flow,TEST_CONNECTOR),testObject,flow,MuleTestUtils.getTestSession(muleContext)));
}
