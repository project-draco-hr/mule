{
  Map<String,Serializable> props=new HashMap<>();
  props.put("object",new Apple());
  props.put("number",1);
  props.put("string","hello");
  testObject=InternalMessage.builder().payload("test").outboundProperties(props).build();
  Flow flow=getTestFlow();
  setCurrentEvent(Event.builder(DefaultEventContext.create(flow,TEST_CONNECTOR)).message(testObject).flow(flow).session(MuleTestUtils.getTestSession(muleContext)).build());
}
