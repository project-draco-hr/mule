{
  try {
    Thread.sleep(5000);
    logger.fatal(messageIfNeedToKill);
    Runtime.getRuntime().halt(1);
  }
 catch (  InterruptedException e) {
    logger.info("Test thread has been interrupted, probably bt the call to watchdog.cancel() in teardown method.",e);
  }
}
