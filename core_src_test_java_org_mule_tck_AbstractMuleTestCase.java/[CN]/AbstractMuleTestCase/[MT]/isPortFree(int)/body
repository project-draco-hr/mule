{
  ServerSocket server=null;
  try {
    server=new ServerSocket(port);
  }
 catch (  IOException e) {
    return false;
  }
 finally {
    try {
      if (server != null) {
        server.close();
      }
    }
 catch (    IOException e) {
    }
    return true;
  }
}
