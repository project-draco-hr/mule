{
  String msg="Timeout of " + unit.toMillis(timeout) + "ms exceeded - exiting VM!";
  if (failOnTimeout) {
    logger.fatal(msg);
    Runtime.getRuntime().halt(1);
  }
 else {
    logger.warn(msg);
  }
}
