{
  String oldFrequency=frequency;
  frequency=newFrequency;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,SchemaPackage.CONSTRAINT_TYPE__FREQUENCY,oldFrequency,frequency));
}
