{
  String oldExpectedType=expectedType;
  expectedType=newExpectedType;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,MulePackage.CONSTRAINT_TYPE__EXPECTED_TYPE,oldExpectedType,expectedType));
}
