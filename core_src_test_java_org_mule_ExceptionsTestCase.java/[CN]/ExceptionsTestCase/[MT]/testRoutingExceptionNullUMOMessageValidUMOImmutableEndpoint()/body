{
  UMOImmutableEndpoint endpoint=managementContext.getRegistry().lookupEndpointFactory().createOutboundEndpoint("test://outbound",managementContext);
  assertNotNull(endpoint);
  RoutingException rex=new RoutingException(null,endpoint);
  assertNotNull(rex);
  assertSame(endpoint,rex.getEndpoint());
}
