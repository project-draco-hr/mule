{
  MuleMessageFactory factory=createMuleMessageFactory();
  InternalMessage message=factory.create(getValidTransportMessage(),encoding);
  assertNotNull(message);
  assertMessageProperties(message);
}
