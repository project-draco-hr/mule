{
  if (!element.getParentNode().getLocalName().equals(ROOT_ELEMENT)) {
    throw new IllegalStateException("This element should be embedded inside the Mule <mule> element: " + MuleHierarchicalBeanDefinitionParserDelegate.elementToString(element));
  }
}
