{
  if (!isTopLevel(element)) {
    throw new IllegalStateException("This element should be embedded inside the Mule <" + ROOT_ELEMENT + "> or <"+ ROOT_UNSAFE_ELEMENT+ "> elements: "+ XmlUtils.elementToString(element));
  }
}
