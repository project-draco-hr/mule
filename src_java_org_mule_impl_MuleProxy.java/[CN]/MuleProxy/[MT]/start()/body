{
  checkDisposed();
  if (!umo.isStarted()) {
    try {
      umo.start();
    }
 catch (    Exception e) {
      throw new ModelException("Failed to start component: " + e.getMessage(),e);
    }
  }
}
