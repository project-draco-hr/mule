{
  MuleMessage originalMessage=eventContext.getEvent().getMessage();
  MuleMessage message=MuleMessage.builder().payload(originalMessage.getPayload()).mediaType(originalMessage.getDataType().getMediaType()).attributes(originalMessage.getAttributes()).build();
  receivedMessages.add(message);
  return eventContext.getEvent();
}
