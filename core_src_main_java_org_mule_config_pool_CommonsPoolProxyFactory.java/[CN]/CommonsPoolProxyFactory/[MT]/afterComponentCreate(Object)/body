{
  try {
    BeanUtils.populate(component,descriptor.getProperties());
  }
 catch (  Exception e) {
    throw new InitialisationException(CoreMessages.failedToSetPropertiesOn("Component '" + descriptor.getName() + "'"),e,descriptor);
  }
}
