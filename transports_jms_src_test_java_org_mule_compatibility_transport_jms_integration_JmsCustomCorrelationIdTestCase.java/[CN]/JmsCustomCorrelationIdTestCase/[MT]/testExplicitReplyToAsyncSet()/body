{
  MuleClient client=muleContext.getClient();
  MuleMessage response=client.send("vm://in4",MuleMessage.builder().payload(TEST_MESSAGE).addOutboundProperty("customCorrelation","abcdefghij").build()).getRight();
  assertEquals(TEST_MESSAGE + " TestService1",response.getPayload());
}
