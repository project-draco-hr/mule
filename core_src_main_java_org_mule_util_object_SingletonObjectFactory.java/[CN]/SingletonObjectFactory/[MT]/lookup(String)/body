{
  if (instance != null && instance instanceof Identifiable && id != null) {
    if (id.equals(((Identifiable)instance).getId())) {
      return instance;
    }
 else {
      return null;
    }
  }
 else {
    return instance;
  }
}
