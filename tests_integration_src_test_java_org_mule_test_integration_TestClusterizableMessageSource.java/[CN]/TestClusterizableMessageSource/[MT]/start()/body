{
  MuleMessage muleMessage=MuleMessage.builder().payload("TEST").build();
  DefaultMuleEvent defaultMuleEvent=new DefaultMuleEvent(create(flowConstruct),muleMessage,ONE_WAY,flowConstruct);
  listener.process(defaultMuleEvent);
}
