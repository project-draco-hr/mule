{
  MuleMessage muleMessage=MuleMessage.builder().payload("TEST").build();
  MuleEvent defaultMuleEvent=MuleEvent.builder(DefaultMessageContext.create(flowConstruct,TEST_CONNECTOR)).message(muleMessage).exchangePattern(ONE_WAY).flow(flowConstruct).build();
  listener.process(defaultMuleEvent);
}
