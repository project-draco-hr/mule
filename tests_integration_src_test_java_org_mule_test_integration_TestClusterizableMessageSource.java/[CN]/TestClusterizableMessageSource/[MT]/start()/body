{
  MuleMessage muleMessage=MuleMessage.builder().payload("TEST").build();
  DefaultMuleEvent defaultMuleEvent=new DefaultMuleEvent(DefaultMessageContext.create(flowConstruct,TEST_CONNECTOR),muleMessage,ONE_WAY,flowConstruct);
  listener.process(defaultMuleEvent);
}
