{
  FlowConstruct flowConstruct=MuleTestUtils.getTestFlow(muleContext);
  return Event.builder(DefaultEventContext.create(flowConstruct,TEST_CONNECTOR)).message(data).exchangePattern(REQUEST_RESPONSE).flow(flowConstruct).session(MuleTestUtils.getTestSession(flowConstruct,muleContext)).build();
}
