{
  try {
    getManagementContext().getLifecycleManager().applyLifecycle(getManagementContext(),object);
  }
 catch (  UMOException e) {
    throw new MuleRuntimeException(CoreMessages.failedToInvokeLifecycle(getManagementContext().getLifecycleManager().getCurrentPhase(),object),e);
  }
}
