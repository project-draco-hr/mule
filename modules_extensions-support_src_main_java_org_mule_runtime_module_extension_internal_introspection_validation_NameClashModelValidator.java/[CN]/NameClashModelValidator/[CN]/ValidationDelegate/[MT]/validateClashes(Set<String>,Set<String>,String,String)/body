{
  Set<String> intersection=Sets.intersection(set1,set2);
  if (!intersection.isEmpty()) {
    throw new IllegalModelDefinitionException(format("Extension '%s' has %s and %s with the same name. Offending names are: [%s]",extensionModel.getName(),type1,type2,Joiner.on(", ").join(intersection)));
  }
}
