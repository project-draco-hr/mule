{
  if (deadLetterQueue instanceof Processor) {
    dlqMP=(Processor)deadLetterQueue;
  }
 else {
    throw new InitialisationException(I18nMessageFactory.createStaticMessage("deadLetterQueue-ref is not a valid mesage processor: " + deadLetterQueue),null,this);
  }
}
