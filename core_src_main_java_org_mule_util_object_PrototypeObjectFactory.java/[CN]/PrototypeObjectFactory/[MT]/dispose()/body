{
  if (instances != null) {
    Iterator it=instances.values().iterator();
    while (it.hasNext()) {
      Object obj=it.next();
      if (obj instanceof Disposable) {
        ((Disposable)obj).dispose();
      }
    }
    instances.clear();
    instances=null;
  }
}
