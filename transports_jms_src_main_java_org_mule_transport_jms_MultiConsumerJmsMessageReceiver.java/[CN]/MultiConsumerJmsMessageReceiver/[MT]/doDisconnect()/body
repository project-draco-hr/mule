{
  logger.debug("doDisconnect()");
  if (consumers != null) {
    SubReceiver sub;
    for (int i=0; i < receiversCount; i++) {
      sub=(SubReceiver)consumers.removeFirst();
      try {
        sub.doDisconnect();
      }
  finally {
        sub=null;
      }
    }
    consumers.clear();
  }
}
