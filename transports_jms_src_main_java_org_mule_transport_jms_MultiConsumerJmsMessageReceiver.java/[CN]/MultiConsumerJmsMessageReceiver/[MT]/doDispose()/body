{
  logger.debug("doDispose()");
  if (session != null) {
    try {
      session.close();
    }
 catch (    Exception e) {
      logger.warn("Failed to close jms session: " + e.getMessage());
    }
 finally {
      session=null;
    }
  }
}
