{
  logger.debug("doConnect()");
  SubReceiver sub;
  for (int i=0; i < receiversCount; i++) {
    sub=new SubReceiver();
    try {
      sub.doConnect();
    }
 catch (    FatalException fex) {
      sub.doDisconnect();
      throw fex;
    }
 finally {
      consumers.addLast(sub);
    }
  }
}
