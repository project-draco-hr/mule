{
  return artifact.getGroupId().equals(anotherArtifact.getGroupId()) && artifact.getArtifactId().equals(anotherArtifact.getArtifactId()) && (isNotEmpty(artifact.getVersion()) ? artifact.getVersion().equals(anotherArtifact.getVersion()) : true);
}
