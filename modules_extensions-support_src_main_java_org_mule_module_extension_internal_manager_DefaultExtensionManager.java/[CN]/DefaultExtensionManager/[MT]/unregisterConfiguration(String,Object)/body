{
  try {
    muleContext.getRegistry().unregisterObject(key);
  }
 catch (  RegistrationException e) {
    LOGGER.error(String.format("Could not unregister expired dynamic config of key '%s' and type %s",key,object.getClass().getName()),e);
  }
}
