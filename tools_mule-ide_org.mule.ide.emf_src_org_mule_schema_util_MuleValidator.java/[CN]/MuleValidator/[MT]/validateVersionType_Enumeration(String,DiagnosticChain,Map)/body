{
  boolean result=VERSION_TYPE__ENUMERATION__VALUES.contains(versionType);
  if (!result && diagnostics != null)   reportEnumerationViolation(MulePackage.eINSTANCE.getVersionType(),versionType,VERSION_TYPE__ENUMERATION__VALUES,diagnostics,context);
  return result;
}
