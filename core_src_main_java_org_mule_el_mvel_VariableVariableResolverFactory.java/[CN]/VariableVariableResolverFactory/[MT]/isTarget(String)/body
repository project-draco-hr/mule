{
  if (message == null) {
    return isNextResolveable(name);
  }
  return message.getInvocationPropertyNames().contains(name) || message.getSessionPropertyNames().contains(name) || isNextResolveable(name);
}
