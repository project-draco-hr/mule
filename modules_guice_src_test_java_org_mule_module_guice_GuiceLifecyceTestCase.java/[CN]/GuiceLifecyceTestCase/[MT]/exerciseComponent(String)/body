{
  MuleClient muleClient=muleContext.getClient();
  final AbstractLifecycleTracker ltc=(AbstractLifecycleTracker)muleClient.send("vm://" + serviceName + ".In",new DefaultMuleMessage(NullPayload.getInstance(),muleContext)).getPayload();
  assertNotNull(ltc);
  return ltc;
}
