{
  if (configFilePath == null) {
    IResource resource=getModel().getProject().findMember(IMuleDefaults.MULE_IDE_CONFIG_FILENAME);
    if (resource != null) {
      configFilePath=resource.getFullPath();
    }
  }
  return configFilePath;
}
