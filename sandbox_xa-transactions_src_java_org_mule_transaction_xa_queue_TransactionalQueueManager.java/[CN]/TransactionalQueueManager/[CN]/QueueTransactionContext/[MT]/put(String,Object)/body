{
  readOnly=false;
  if (added == null) {
    added=new HashMap();
  }
  List queueAdded=(List)added.get(queueName);
  if (queueAdded == null) {
    queueAdded=new ArrayList();
    added.put(queueName,queueAdded);
  }
  queueAdded.add(item);
}
