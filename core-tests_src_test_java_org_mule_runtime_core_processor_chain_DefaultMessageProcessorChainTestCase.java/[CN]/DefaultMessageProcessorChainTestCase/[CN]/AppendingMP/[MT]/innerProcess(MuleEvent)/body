{
  this.event=event;
  MuleEvent result=MuleEvent.builder(event).message(MuleMessage.builder().payload(event.getMessage().getPayload() + appendString).build()).build();
  this.resultEvent=result;
  return result;
}
