{
  final MuleClient client=new MuleClient();
  for (int i=0; i < 5; i++) {
    MuleMessage result=client.send("vm://receive",new DefaultMuleMessage(new Object[]{"USD","EUR"}));
    assertNotNull(result.getPayload());
  }
}
