{
  header.add("Application: " + context.getConfiguration().getId());
  header.add(String.format("OS encoding: %s, Mule encoding: %s",System.getProperty("file.encoding"),context.getConfiguration().getDefaultEncoding()));
  header.add(" ");
}
