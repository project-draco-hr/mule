{
  return operationContext.getTransactionConfig().isPresent() ? getTransactedConnectionHandler(operationContext,operationContext.getTransactionConfig().get()) : getTransactionlessConnectionHandler(operationContext);
}
