{
  try {
    invocation.setMessage(new DefaultMuleMessage(invocation.getMessage().getPayloadAsString() + name + AFTER));
  }
 catch (  Exception e) {
    e.printStackTrace();
    fail(e.getMessage());
  }
}
