{
  try {
    invocation.setMessage(new DefaultMuleMessage(invocation.getMessage().getPayloadAsString() + name + BEFORE,muleContext));
  }
 catch (  Exception e) {
    e.printStackTrace();
    fail(e.getMessage());
  }
}
