{
  if (args.length == 1 && args[0].equals("?")) {
    RunnerConfig.usage();
    System.exit(0);
  }
  try {
    Runner runner=new Runner(new RunnerConfig(args));
    runner.start();
  }
 catch (  Throwable e) {
    e.printStackTrace();
  }
}
