{
  MetadataResult<ComponentMetadataDescriptor> metadata=getMetadata("selectMetadata","select * from PLANET");
  assertThat(metadata.isSuccess(),is(true));
  assertThat(metadata.get().getInputMetadata().get().getParameterMetadata("inputParameters").get().getType(),is(typeBuilder.nullType().build()));
}
