{
  String[][] groups=new String[][]{new String[]{"a1"}};
  String text="must have all attributes for one of the sets: [a1]";
  assertBad(groups,"",text);
  assertBad(groups,"x",text);
  assertOk(groups,"a1");
  assertOk(groups,"a1 x");
  assertOk(groups,"x a1");
}
