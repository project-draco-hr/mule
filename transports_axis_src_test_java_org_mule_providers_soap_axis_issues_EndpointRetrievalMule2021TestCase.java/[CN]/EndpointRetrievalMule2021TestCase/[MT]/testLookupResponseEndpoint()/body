{
  UMOImmutableEndpoint endpoint1=managementContext.getRegistry().lookupEndpointFactory().getResponseEndpoint("Endpoint",managementContext);
  assertEndpointOk(endpoint1);
  UMOImmutableEndpoint endpoint2=managementContext.getRegistry().lookupEndpointFactory().getResponseEndpoint("axis:http://localhost:18081/mule/Service?method=toString",managementContext);
  assertEndpointOk(endpoint2);
}
