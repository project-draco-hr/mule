{
  Object[] payload=(Object[])src;
  Book book=(Book)payload[0];
  String address=(String)payload[1];
  String email=(String)payload[2];
  String body="Thank you for placing your order for " + book.getTitle() + " with Mule Bookstore. "+ "Your order will be shipped  to "+ address+ " by 3 PM today!";
  RequestContext.getEventContext().getMessage().setProperty(MailProperties.TO_ADDRESSES_PROPERTY,email);
  System.out.println("Sent email to " + email);
  return body;
}
