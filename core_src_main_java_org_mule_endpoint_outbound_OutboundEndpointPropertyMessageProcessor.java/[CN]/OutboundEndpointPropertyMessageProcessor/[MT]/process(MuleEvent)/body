{
  event.getMessage().setOutboundProperty(MuleProperties.MULE_ENDPOINT_PROPERTY,event.getEndpoint().getEndpointURI().toString());
  event=OptimizedRequestContext.unsafeSetEvent(event);
  return event;
}
