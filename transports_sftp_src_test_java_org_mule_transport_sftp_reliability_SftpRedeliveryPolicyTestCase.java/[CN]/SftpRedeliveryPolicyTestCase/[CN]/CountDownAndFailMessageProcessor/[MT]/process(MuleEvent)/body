{
  String payload=null;
  try {
    payload=event.getMessage().getPayloadAsString();
  }
 catch (  Exception e) {
  }
  if (FAIL.equals(payload)) {
    latch.countDown();
    throw new RuntimeException();
  }
  return event;
}
