{
  if (operationMode != OPERATION_MODE_STARTED && operationMode != OPERATION_MODE_STOPPING) {
    throw new ResourceManagerSystemException("FileManager not ready");
  }
  if (dirty) {
    throw new ResourceManagerSystemException("Transaction manager is set to dirty, this *may* mean it is corrupt. No modifications are allowed until a recovery run has been performed!");
  }
}
