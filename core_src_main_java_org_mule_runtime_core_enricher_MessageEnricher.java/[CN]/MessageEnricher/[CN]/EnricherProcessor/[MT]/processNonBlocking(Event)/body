{
  this.eventToEnrich=event;
  Event result=processNext(copyEventForEnrichment(Event.builder(event).replyToHandler(createReplyToHandler(event)).build()));
  if (!(result instanceof NonBlockingVoidMuleEvent)) {
    result=processResponse(result,event);
  }
  return result;
}
