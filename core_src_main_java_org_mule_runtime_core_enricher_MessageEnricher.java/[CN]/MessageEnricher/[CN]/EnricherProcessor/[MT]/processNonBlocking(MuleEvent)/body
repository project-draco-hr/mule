{
  this.eventToEnrich=event;
  MuleEvent result=processNext(copyEventForEnrichment(MuleEvent.builder(event).replyToHandler(createReplyToHandler(event)).build()));
  if (!(result instanceof NonBlockingVoidMuleEvent)) {
    result=processResponse(result,event);
  }
  return result;
}
