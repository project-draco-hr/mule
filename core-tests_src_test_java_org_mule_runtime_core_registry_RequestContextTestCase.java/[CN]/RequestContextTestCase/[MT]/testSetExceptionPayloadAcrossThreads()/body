{
  MuleEvent event=MuleEvent.builder(DefaultMessageContext.create(flow,TEST_CONNECTOR)).message(MuleMessage.builder().payload("").build()).build();
  runThread(event,false);
  runThread(event,true);
}
