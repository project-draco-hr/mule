{
  if (!stopped.get()) {
    try {
      connected.whenTrue(null);
      this.poll();
    }
 catch (    InterruptedException e) {
      this.stop();
    }
catch (    Exception e) {
      this.handleException(e);
    }
  }
}
