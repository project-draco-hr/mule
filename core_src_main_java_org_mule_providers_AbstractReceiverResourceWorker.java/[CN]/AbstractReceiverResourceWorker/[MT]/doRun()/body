{
  try {
    Object message=getNextMessage(resource);
    while (message != null) {
      messages.add(message);
      super.doRun();
      message=getNextMessage(resource);
    }
  }
 catch (  Exception e) {
    handleException(e);
  }
}
