{
  String oldClassName=className;
  className=newClassName;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,SchemaPackage.COMPONENT_POOL_FACTORY_TYPE__CLASS_NAME,oldClassName,className));
}
