{
  Set mbeans=getMBeans();
  Iterator it=mbeans.iterator();
  List mbeanClasses=new ArrayList();
  while (it.hasNext()) {
    mbeanClasses.add(((ObjectInstance)it.next()).getClassName());
  }
  return mbeanClasses;
}
