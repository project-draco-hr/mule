{
  if (endpoint == null)   return;
  String name=endpoint.getName();
  Integer cpt=(Integer)routed.get(name);
  int count=0;
  if (cpt != null)   count=cpt.intValue();
  count++;
  routed.put(name,new Integer(count));
  totalRouted++;
  totalReceived++;
}
