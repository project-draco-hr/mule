{
  if (!disposed.get()) {
    try {
      try {
        this.disconnect();
      }
 catch (      Exception e) {
        logger.warn(e.getMessage(),e);
      }
      disposing.set(true);
      this.doDispose();
    }
  finally {
      disposed.set(true);
    }
  }
}
