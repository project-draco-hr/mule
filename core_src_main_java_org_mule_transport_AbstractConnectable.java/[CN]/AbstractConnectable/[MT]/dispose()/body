{
  if (!disposed) {
    try {
      try {
        this.disconnect();
      }
 catch (      Exception e) {
        logger.warn(e.getMessage(),e);
      }
      this.doDispose();
    }
  finally {
      disposed=true;
    }
  }
}
