{
  if (!connected.get()) {
    startOnConnect=true;
    try {
      connect();
    }
 catch (    Exception e) {
      throw new LifecycleException(e,this);
    }
    return;
  }
  if (stopped.compareAndSet(true,false)) {
    doStart();
  }
}
