{
  try {
    if (connected.get()) {
      disconnect();
    }
  }
 catch (  Exception e) {
    logger.error(e.getMessage(),e);
  }
  if (started.compareAndSet(true,false)) {
    try {
      doStop();
    }
 catch (    MuleException e) {
      logger.error(e.getMessage(),e);
    }
  }
}
