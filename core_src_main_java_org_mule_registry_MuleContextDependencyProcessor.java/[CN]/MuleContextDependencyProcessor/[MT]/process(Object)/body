{
  if (object instanceof MuleContextAware) {
    if (context == null) {
      if (context == null) {
        return object;
      }
    }
    ((MuleContextAware)object).setMuleContext(context);
  }
  return object;
}
