{
  event=Event.builder(event).message(InternalMessage.builder(event.getMessage()).addOutboundProperty(LIFECYCLE_TRACKER_PROCESSOR_PROPERTY,getTracker().toString()).build()).addVariable(FLOW_CONSRUCT_PROPERTY,flowConstruct).build();
  return event;
}
