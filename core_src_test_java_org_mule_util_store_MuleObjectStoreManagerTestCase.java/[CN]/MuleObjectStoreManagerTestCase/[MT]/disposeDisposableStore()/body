{
  @SuppressWarnings("unchecked") ObjectStore<Serializable> store=Mockito.mock(ObjectStore.class,Mockito.withSettings().extraInterfaces(Disposable.class));
  storeManager.disposeStore(store);
  Mockito.verify(store).clear();
  Mockito.verify((Disposable)store).dispose();
}
