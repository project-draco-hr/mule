{
  Object data=event.getMessage().getPayload();
  if (!(data instanceof Message)) {
    throw new DispatchException(CoreMessages.transformUnexpectedType(data.getClass(),Message.class),event,event.getEndpoint());
  }
 else {
    sendMailMessage((Message)data);
  }
}
