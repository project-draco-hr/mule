{
  String oldClassName=className;
  className=newClassName;
  if (eNotificationRequired())   eNotify(new ENotificationImpl(this,Notification.SET,SchemaPackage.COMPONENT_LIFECYCLE_ADAPTER_FACTORY_TYPE__CLASS_NAME,oldClassName,className));
}
