{
  Map<String,Serializable> properties=new HashMap<>();
  properties.put(CONTENT_ENCODING,"gzip,deflate");
  MuleMessage response=muleContext.getClient().send("http://localhost:" + httpPortProxy.getNumber() + "/proxy",MuleMessage.builder().payload(gzip(getAllRequest)).outboundProperties(properties).build(),HTTP_REQUEST_OPTIONS).getRight();
  validateResponse(response);
}
