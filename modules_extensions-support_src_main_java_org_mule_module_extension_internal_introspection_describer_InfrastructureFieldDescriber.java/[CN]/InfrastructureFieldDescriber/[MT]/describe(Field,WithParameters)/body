{
  ParameterDescriptor descriptor=field.getAnnotation(Optional.class) != null ? with.optionalParameter(attributeName) : with.requiredParameter(attributeName);
  return descriptor.ofType(clazz).withExpressionSupport(ExpressionSupport.NOT_SUPPORTED).withModelProperty(new DeclaringMemberModelProperty(field));
}
