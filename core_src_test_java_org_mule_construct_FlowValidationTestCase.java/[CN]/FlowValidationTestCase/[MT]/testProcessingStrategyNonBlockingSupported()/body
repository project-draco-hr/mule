{
  flow.setProcessingStrategy(new NonBlockingProcessingStrategy());
  flow.setMessageSource(new NonBlockingMessageSource(){
    @Override public void setListener(    MessageProcessor listener){
    }
  }
);
  flow.validateConstruct();
}
