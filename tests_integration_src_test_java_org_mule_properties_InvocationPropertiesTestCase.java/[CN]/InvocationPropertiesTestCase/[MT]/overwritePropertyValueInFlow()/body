{
  FlowRunner runner=flowRunner("OverwritePropertyValueInFlow").withPayload("data").withFlowVariable("P1","P1_VALUE").asynchronously();
  MuleEvent event=runner.buildEvent();
  runner.run();
  assertThat(event.getMessage().getProperty("P1",PropertyScope.INVOCATION),is("P1_VALUE_NEW"));
}
