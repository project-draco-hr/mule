{
  MessagingException e=flowRunner(flowName).withPayload(TEST_MESSAGE).withVariable("code",statusCode).runExpectingException();
  assertThat(e.getEvent().getError().get().getCause(),instanceOf(ResponseValidatorException.class));
}
