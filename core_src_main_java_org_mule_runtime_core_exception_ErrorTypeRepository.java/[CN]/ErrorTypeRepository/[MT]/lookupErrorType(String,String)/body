{
  ErrorType errorType=this.errorTypes.get(new ErrorTypeKey(namespace,stringRepresentation));
  if (errorType == null) {
    throw new IllegalStateException(format("there's no error type with namespace %s and string representation",namespace,stringRepresentation));
  }
  return errorType;
}
