{
  if (this == obj)   return true;
  if (obj == null || getClass() != obj.getClass())   return false;
  final MuleTransactionConfig other=(MuleTransactionConfig)obj;
  return ClassUtils.equal(factory,other.factory) && ClassUtils.equal(new Byte(action),new Byte(other.action)) && ClassUtils.equal(constraint,other.constraint)&& ClassUtils.equal(new Integer(timeout),new Integer(other.timeout));
}
