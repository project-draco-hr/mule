{
  if (contentParameter.isPresent()) {
    ParameterMetadataDescriptor descriptor=parameterDescriptor(contentParameter.get().getName()).withType(contentParameter.get().getType()).build();
    return Optional.of(success(descriptor));
  }
  return Optional.empty();
}
