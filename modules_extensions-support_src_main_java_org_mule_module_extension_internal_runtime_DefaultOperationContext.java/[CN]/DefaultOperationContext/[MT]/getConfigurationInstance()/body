{
  if (configurationInstance == null) {
    configurationInstance=StringUtils.isBlank(configurationInstanceProviderName) ? extensionManager.getConfigurationInstance(extension,this) : extensionManager.getConfigurationInstance(extension,configurationInstanceProviderName,this);
  }
  return (C)configurationInstance;
}
