{
  if (configuration == null) {
    configuration=StringUtils.isBlank(configurationProviderName) ? extensionManager.getConfiguration(extensionModel,this) : extensionManager.getConfiguration(extensionModel,configurationProviderName,this);
  }
  return (C)configuration;
}
