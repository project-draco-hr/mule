{
  LocalMuleClient client=muleContext.getClient();
  MuleMessage result;
  result=client.send("http://localhost:9002/baz",getTestMuleMessage(),newOptions().method(org.mule.module.http.api.HttpConstants.Methods.POST.name()).build());
  assertEquals("test received",result.getPayloadAsString());
}
