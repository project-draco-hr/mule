{
  xaSessionProducer=new DefaultXaSessionResourceProducer("uniqueName",Mockito.mock(AbstractXAResourceManager.class));
  transactionManagerWrapper=new TransactionManagerWrapper(Mockito.mock(TransactionManager.class,Answers.RETURNS_DEEP_STUBS.get()),xaSessionProducer);
}
