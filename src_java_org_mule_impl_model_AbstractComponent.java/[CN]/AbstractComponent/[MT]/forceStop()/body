{
  if (!stopped.get()) {
    logger.debug("Stopping UMOComponent");
    stopping.set(true);
    doForceStop();
    stopped.set(true);
    stopping.set(false);
    fireComponentEvent(ComponentEvent.COMPONENT_STOPPED);
  }
}
